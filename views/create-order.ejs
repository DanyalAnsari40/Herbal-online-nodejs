<!DOCTYPE html>
<html lang="en">

<%- include('partials/modern-styles', { pageTitle: 'Elite Create Order' }) %>

<body class="bg-white font-inter min-h-screen">
  <div class="min-h-screen flex flex-col">
    <% if (user.role==='admin' ) { %>
      <div class="min-h-screen flex">
        <%- include('partials/modern-sidebar') %>


        <div class="flex-1 flex flex-col">
          <%- include('partials/modern-header', { pageTitle: 'Create Order' }) %>
          <% } else { %>
            <!-- Employee header with special style -->
            <header class="bg-gradient-to-r from-green-600 via-green-700 to-indigo-700 text-white shadow-lg">
              <div class="container mx-auto px-6 py-4">
                <div class="flex justify-between items-center">
                  <div class="flex items-center space-x-4">
                    <div class="w-24 h-24 rounded-full overflow-hidden border-2 border-white">
                      <img src="/uploads/<%= user.profilePic || 'default-profile.png' %>" alt="Profile"
                        class="w-full h-full object-cover">
                    </div>
                    <div>
                      <h1
                        class="text-3xl font-bold bg-gradient-to-r from-white to-green-100 bg-clip-text text-transparent">
                        Dashboard</h1>
                      <p class="text-green-100 text-sm flex items-center mt-1">
                        <i class="fas fa-user-circle mr-2"></i>
                        Welcome, <%= user.email %>
                          <span class="ml-2 px-2 py-1 bg-white/20 rounded-full text-xs font-medium">
                            <%= user.role %>
                          </span>
                      </p>
                    </div>
                  </div>
                  <div class="flex items-center space-x-4">
                    <button onclick="document.getElementById('profileModal').classList.remove('hidden')"
                      class="text-white hover:text-green-200 text-xl transition">
                      <i class="fas fa-cog"></i>
                    </button>
                    <a href="/logout"
                      class="flex items-center space-x-2 bg-white/10 hover:bg-white/20 px-4 py-2 rounded-lg transition-all duration-200 backdrop-blur-sm">
                      <i class="fas fa-sign-out-alt"></i>
                      <span>Logout</span>
                    </a>
                  </div>

                </div>

                <!-- Navigation -->
                <nav class="mt-6 flex space-x-1">
                  <a href="/admin"
                    class="<% if (currentRoute === 'admin') { %>bg-white/20 text-white border-b-2 border-white<% } else { %>text-green-100 hover:text-white hover:bg-white/10<% } %> px-4 py-2 rounded-lg transition-all duration-200 flex items-center space-x-2">
                    <i class="fas fa-home"></i>
                    <span>Dashboard</span>
                  </a>

                  <% if (user.role==='admin' || user.permissions.includes('orders')) { %>
                    <a href="/admin/orders"
                      class="<% if (currentRoute === 'orders') { %>bg-white/20 text-white border-b-2 border-white<% } else { %>text-green-100 hover:text-white hover:bg-white/10<% } %> px-4 py-2 rounded-lg transition-all duration-200 flex items-center space-x-2">
                      <i class="fas fa-shopping-cart"></i>
                      <span>Orders</span>
                    </a>
                    <% } %>

                      <% if (user.role==='admin' || user.permissions.includes('create-order')) { %>
                        <a href="/admin/create-order"
                          class="<% if (currentRoute === 'create-order') { %>bg-white/20 text-white border-b-2 border-white<% } else { %>text-green-100 hover:text-white hover:bg-white/10<% } %> px-4 py-2 rounded-lg transition-all duration-200 flex items-center space-x-2">
                          <i class="fas fa-plus-circle"></i>
                          <span>Create Order</span>
                        </a>
                        <% } %>

                          <% if (user.role==='admin' || user.permissions.includes('employee-management')) { %>
                            <a href="/admin/employee-management"
                              class="<% if (currentRoute === 'employee-management') { %>bg-white/20 text-white border-b-2 border-white<% } else { %>text-green-100 hover:text-white hover:bg-white/10<% } %> px-4 py-2 rounded-lg transition-all duration-200 flex items-center space-x-2">
                              <i class="fas fa-users"></i>
                              <span>Employee Management</span>
                            </a>
                            <% } %>

                              <% if (user.role==='admin' || user.permissions.includes('track-product')) { %>
                                <a href="/admin/tracking"
                                  class="<% if (currentRoute === 'tracking') { %>bg-white/20 text-white border-b-2 border-white<% } else { %>text-green-100 hover:text-white hover:bg-white/10<% } %> px-4 py-2 rounded-lg transition-all duration-200 flex items-center space-x-2">
                                  <i class="fa-solid fa-truck"></i>
                                  <span>Track Product</span>
                                </a>
                                <% } %>

                                  <% if (user.role==='admin' || user.permissions.includes('product-management')) { %>
                                    <a href="/admin/product-management"
                                      class="<% if (currentRoute === 'product-management') { %>bg-white/20 text-white border-b-2 border-white<% } else { %>text-green-100 hover:text-white hover:bg-white/10<% } %> px-4 py-2 rounded-lg transition-all duration-200 flex items-center space-x-2">
                                      <i class="fas fa-boxes"></i>
                                      <span>Product Management</span>
                                    </a>
                                    <% } %>
                                      <% if (user.role==='admin' || user.permissions.includes('finance')) { %>
                                        <a href="/admin/finance"
                                          class="<%= currentRoute === 'finance' ? 'bg-white/20 text-white border-b-2 border-white' : 'text-green-100 hover:text-white hover:bg-white/10' %> px-4 py-2 rounded-lg flex items-center space-x-2">
                                          <i class="fas fa-coins"></i>
                                          <span>Finance</span>
                                        </a>
                                        <% } %>

                </nav>

              </div>
            </header>

            <% } %>


              <!-- Modern Main Content -->
              <main class="container mx-auto px-8 py-12 flex-grow">
                <% if (message) { %>
                  <div
                    class="mb-8 p-6 text-center glass rounded-2xl shadow-lg border-l-4 <%= message.includes('successfully') ? 'success-glow border-green-400' : 'error-glow border-red-400' %>">
                    <div class="flex items-center justify-center space-x-2">
                      <i
                        class="fas <%= message.includes('successfully') ? 'fa-check-circle' : 'fa-exclamation-circle' %>"></i>
                      <span class="font-medium">
                        <%= message %>
                      </span>
                    </div>
                  </div>
                  <% } %>

                    <!-- Modern Create Order Form -->
                    <section class="mb-12">
                      <div class="mb-10 glass rounded-3xl p-8 shadow-2xl">
                        <h2 class="text-4xl font-bold mb-4 gradient-text">✨ Create New Order</h2>
                        <p class="text-lg font-medium" style="color: #14213D;">Add a new customer order with detailed information</p>
                        <div class="mt-4 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                          <button onclick="openTcsModal()"
                            class="group flex items-center justify-center px-8 py-8 rounded-2xl stats-card shadow-2xl card-hover">
                            <i class="fas fa-shipping-fast mr-3 text-3xl group-hover:scale-110 transition-transform" style="color: #FCA311;"></i>
                            <span class="text-xl font-bold" style="color: #14213D;">TCS</span>
                          </button>
                          <button onclick="openLeopardsModal()"
                            class="group flex items-center justify-center px-8 py-8 rounded-2xl stats-card shadow-2xl card-hover">
                            <i class="fas fa-truck mr-3 text-3xl group-hover:scale-110 transition-transform" style="color: #FCA311;"></i>
                            <span class="text-xl font-bold" style="color: #14213D;">Leopards</span>
                          </button>
                          <button onclick="openMnpModal()"
                            class="group flex items-center justify-center px-8 py-8 rounded-2xl stats-card shadow-2xl card-hover">
                            <i class="fas fa-shipping-fast mr-3 text-3xl group-hover:scale-110 transition-transform" style="color: #FCA311;"></i>
                            <span class="text-xl font-bold" style="color: #14213D;">M&P</span>
                          </button>
                          <button onclick="openPostexModal()"
                            class="group flex items-center justify-center px-8 py-8 rounded-2xl stats-card shadow-2xl card-hover">
                            <i class="fas fa-plane mr-3 text-3xl group-hover:scale-110 transition-transform" style="color: #FCA311;"></i>
                            <span class="text-xl font-bold" style="color: #14213D;">PostEx</span>
                          </button>
                        </div>
                      </div>
                      



                      <!-- Modern Order List -->
                      <section>
                        <div class="mb-8 glass rounded-3xl p-8 shadow-2xl">
                          <h2 class="text-3xl font-bold mb-2 gradient-text">📦 Courier Results & Orders</h2>
                          <p class="text-lg font-medium" style="color: #14213D;">View courier data and orders from different services</p>
                        </div>

                        <!-- Modern Courier Tabs -->
                        <div class="mb-8 glass rounded-2xl p-6 shadow-lg">
                          <div class="border-b" style="border-color: #FCA311;">
                            <nav class="-mb-px flex space-x-8">
                              <button onclick="showTab('tcs')" id="tcs-tab"
                                class="tab-button py-3 px-4 border-b-2 border-transparent font-medium text-sm rounded-t-lg transition-all duration-300 hover:bg-white/10"
                                style="color: #14213D;">
                                <i class="fas fa-shipping-fast mr-2"></i>TCS
                              </button>
                              <button onclick="showTab('leopards')" id="leopards-tab"
                                class="tab-button py-3 px-4 border-b-2 border-transparent font-medium text-sm rounded-t-lg transition-all duration-300 hover:bg-white/10"
                                style="color: #14213D;">
                                <i class="fas fa-truck mr-2"></i>Leopards
                              </button>
                              <button onclick="showTab('mp')" id="mp-tab"
                                class="tab-button py-3 px-4 border-b-2 border-transparent font-medium text-sm rounded-t-lg transition-all duration-300 hover:bg-white/10"
                                style="color: #14213D;">
                                <i class="fas fa-shipping-fast mr-2"></i>M&P
                              </button>
                              <button onclick="showTab('postex')" id="postex-tab"
                                class="tab-button py-3 px-4 border-b-2 border-transparent font-medium text-sm rounded-t-lg transition-all duration-300 hover:bg-white/10"
                                style="color: #14213D;">
                                <i class="fas fa-plane mr-2"></i>PostEx
                              </button>
                            </nav>
                          </div>
                        </div>

                        <!-- Tab Content -->
                        <div id="tab-content" class="space-y-6">
                          <!-- TCS Tab -->
                          <div id="tcs-content" class="tab-content">
                            <div class="bg-white p-6 rounded-2xl shadow-lg border border-gray-100">
                              <h3 class="text-xl font-semibold text-gray-800 mb-4">
                                <i class="fas fa-shipping-fast mr-2 text-orange-500"></i>TCS Orders
                              </h3>
                              <p class="text-gray-600">TCS courier service data will be displayed here.</p>
                            </div>
                          </div>

                          <!-- Leopards Tab -->
                          <div id="leopards-content" class="tab-content hidden">
                            <div class="bg-white p-6 rounded-2xl shadow-lg border border-gray-100">
                              <div class="flex items-center justify-between mb-6">
                                <h3 class="text-xl font-semibold text-gray-800">
                                  <i class="fas fa-truck mr-2 text-green-500"></i>Leopards Shipment Data
                                </h3>
                                <button onclick="fetchLeopardsData()"
                                  class="px-4 py-2 bg-green-500 hover:bg-green-600 text-white rounded-lg font-semibold transition-colors">
                                  <i class="fas fa-sync-alt mr-2"></i>Refresh Data
                                </button>
                              </div>

                              <!-- Filters -->
                              <div class="mb-6 p-4 bg-gray-50 rounded-lg">
                                <h4 class="font-semibold text-gray-700 mb-3">Filters</h4>
                                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                                  <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">From Date</label>
                                    <input type="date" id="from-date" value="2019-12-31"
                                      class="w-full p-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500">
                                  </div>
                                  <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">To Date</label>
                                    <input type="date" id="to-date" value="2025-12-31"
                                      class="w-full p-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500">
                                  </div>
                                  <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">Search</label>
                                    <input type="text" id="search-filter" placeholder="Search shipments..."
                                      class="w-full p-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500">
                                  </div>
                                </div>
                                <div class="mt-3">
                                  <button onclick="applyFilters()"
                                    class="px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white rounded-lg font-semibold transition-colors">
                                    <i class="fas fa-filter mr-2"></i>Apply Filters
                                  </button>
                                  <button onclick="clearFilters()"
                                    class="ml-2 px-4 py-2 bg-gray-500 hover:bg-gray-600 text-white rounded-lg font-semibold transition-colors">
                                    <i class="fas fa-times mr-2"></i>Clear
                                  </button>
                                </div>
                              </div>

                              <!-- Results -->
                              <div id="leopards-result" class="overflow-x-auto">
                                <div class="text-center text-gray-500 py-8">
                                  <i class="fas fa-database text-4xl mb-3"></i>
                                  <p>Click "Refresh Data" to load Leopards shipment information</p>
                                </div>
                              </div>
                            </div>
                          </div>

                          <!-- M&P Tab -->
                          <div id="mp-content" class="tab-content hidden">
                            <div class="bg-white p-6 rounded-2xl shadow-lg border border-gray-100">
                              <div class="flex items-center justify-between mb-6">
                                <h3 class="text-xl font-semibold text-gray-800">
                                  <i class="fas fa-shipping-fast mr-2 text-blue-500"></i>M&P Orders (QSR Report)
                                </h3>
                                <button onclick="fetchMnpReport()"
                                  class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-semibold transition-colors">
                                  <i class="fas fa-sync-alt mr-2"></i>Refresh Report
                                </button>
                              </div>

                              <!-- Filters -->
                              <div class="mb-6 p-4 bg-gray-50 rounded-lg">
                                <h4 class="font-semibold text-gray-700 mb-3">Filters</h4>
                                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                                  <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">locationID</label>
                                    <input type="text" id="mnp-location" value="29609"
                                      class="w-full p-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">
                                  </div>
                                  <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">Year</label>
                                    <input type="number" id="mnp-year" value="2025" min="2017" max="2025"
                                      class="w-full p-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">
                                  </div>
                                  <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">MonthNumber</label>
                                    <input type="number" id="mnp-month" value="1" min="1" max="12"
                                      class="w-full p-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">
                                  </div>
                                </div>
                                <div class="mt-3">
                                  <button onclick="fetchMnpReport()"
                                    class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-semibold transition-colors">
                                    <i class="fas fa-filter mr-2"></i>Apply
                                  </button>
                                  <button onclick="clearMnpFilters()"
                                    class="ml-2 px-4 py-2 bg-gray-500 hover:bg-gray-600 text-white rounded-lg font-semibold transition-colors">
                                    <i class="fas fa-times mr-2"></i>Clear
                                  </button>
                                </div>
                              </div>

                              <!-- Results -->
                              <div id="mnp-result" class="overflow-x-auto">
                                <div class="text-center text-gray-500 py-8">
                                  <i class="fas fa-database text-4xl mb-3"></i>
                                  <p>Set filters and click "Refresh Report" to load M&P QSR data</p>
                                </div>
                              </div>
                            </div>
                          </div>

                          <!-- PostEx Tab -->
                          <div id="postex-content" class="tab-content hidden">
                            <div class="bg-white p-6 rounded-2xl shadow-lg border border-gray-100">
                              <div class="flex items-center justify-between mb-6">
                                <h3 class="text-xl font-semibold text-gray-800">
                                  <i class="fas fa-plane mr-2 text-purple-500"></i>PostEx Orders
                                </h3>
                                <button form="postexResultsForm" type="submit"
                                  class="px-4 py-2 bg-purple-600 hover:bg-purple-700 text-white rounded-lg font-semibold transition-colors">
                                  <i class="fas fa-sync-alt mr-2"></i>Load Orders
                                </button>
                              </div>

                              <!-- Filters -->
                              <div class="mb-6 p-4 bg-gray-50 rounded-lg">
                                <h4 class="font-semibold text-gray-700 mb-3">Filters</h4>
                                <form id="postexResultsForm" class="grid grid-cols-1 md:grid-cols-4 gap-4">
                                  <div>
                                    <label class="block text-sm font-medium mb-1">Order Status</label>
                                    <select id="postexOrderStatusID" class="w-full border rounded px-3 py-2">
                                      <option value="0">All</option>
                                      <option value="1">Unbooked</option>
                                      <option value="2">Booked</option>
                                      <option value="3">PostEx Warehouse</option>
                                      <option value="4">Out For Delivery</option>
                                      <option value="5">Delivered</option>
                                      <option value="6">Returned</option>
                                      <option value="7">Un-Assigned By Me</option>
                                      <option value="8">Expired</option>
                                      <option value="9">Delivery Under Review</option>
                                      <option value="15">Picked By PostEx</option>
                                      <option value="16">Out For Return</option>
                                      <option value="17">Attempted</option>
                                      <option value="18">En-Route to PostEx warehouse</option>
                                    </select>
                                  </div>
                                  <div>
                                    <label class="block text-sm font-medium mb-1">From Date</label>
                                    <input id="postexFromDate" type="date" class="w-full border rounded px-3 py-2" />
                                  </div>
                                  <div>
                                    <label class="block text-sm font-medium mb-1">To Date</label>
                                    <input id="postexToDate" type="date" class="w-full border rounded px-3 py-2" />
                                  </div>
                                  <div class="flex items-end">
                                    <button type="submit" class="px-4 py-2 bg-purple-600 text-white rounded hover:bg-purple-700">Load Orders</button>
                                  </div>
                                </form>
                              </div>

                              <!-- Results -->
                              <div class="overflow-auto">
                                <table class="min-w-full text-sm">
                                  <thead class="bg-gray-100 text-gray-700">
                                    <tr>
                                      <th class="px-3 py-2 text-left">Tracking #</th>
                                      <th class="px-3 py-2 text-left">Customer</th>
                                      <th class="px-3 py-2 text-left">Phone</th>
                                      <th class="px-3 py-2 text-left">City</th>
                                      <th class="px-3 py-2 text-left">Address</th>
                                      <th class="px-3 py-2 text-left">Amount</th>
                                      <th class="px-3 py-2 text-left">Status</th>
                                      <th class="px-3 py-2 text-left">Ref #</th>
                                      <th class="px-3 py-2 text-left">Items</th>
                                      <th class="px-3 py-2 text-left">Invoice Div.</th>
                                      <th class="px-3 py-2 text-left">Date</th>
                                    </tr>
                                  </thead>
                                  <tbody id="postexResultsBody">
                                    <tr><td class="px-3 py-2" colspan="11">No data</td></tr>
                                  </tbody>
                                </table>
                              </div>
                            </div>
                          </div>
                        </div>
                      </section>
              </main>

              <!-- Footer -->
              <footer class="bg-gray-900 text-gray-300 py-6 mt-12">
                <div class="container mx-auto px-6 text-center">
                  <div class="flex items-center justify-center space-x-4 mb-2">
                    <i class="fas fa-shield-alt text-green-400"></i>
                    <span class="font-semibold text-white">Admin Panel</span>
                    <i class="fas fa-shield-alt text-green-400"></i>
                  </div>
                  <p class="text-sm"> 2025 Admin Panel. All rights reserved. | Secure • Reliable • Efficient</p>
                </div>
              </footer>
        </div>
        <!--!  -->
        <!-- TRACKING ID MODAL -->
        <div id="trackingIdModal"
          class="modal-overlay hidden fixed inset-0 bg-black/50 z-50 flex items-center justify-center">
          <div class="bg-white rounded-xl w-full max-w-md shadow-lg p-6 relative">
            <h2 class="text-xl font-bold text-gray-800 mb-4">Enter Tracking ID</h2>
            <input type="text" id="trackingInputField" class="w-full p-3 border border-gray-300 rounded-xl mb-4"
              placeholder="Enter Tracking ID">
            <div class="flex justify-end gap-4">
              <button onclick="closeTrackingModal()"
                class="px-4 py-2 rounded-xl bg-gray-200 hover:bg-gray-300">Cancel</button>
              <button onclick="saveTrackingId()"
                class="px-4 py-2 rounded-xl bg-green-600 hover:bg-green-700 text-white">Save</button>
            </div>
          </div>
        </div>

        <!-- TCS Courier Booking Modal -->
        <div id="tcsModal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
          <div class="bg-white rounded-2xl p-8 w-full max-w-5xl max-h-[90vh] overflow-y-auto shadow-xl">
            <div class="flex justify-between items-center mb-6">
              <h2 class="text-2xl font-bold text-gray-800 flex items-center">
                <i class="fas fa-shipping-fast mr-3 text-red-600"></i>
                TCS Courier Booking
              </h2>
              <button onclick="closeTcsModal()" class="text-gray-500 hover:text-gray-700 text-2xl">
                <i class="fas fa-times"></i>
              </button>
            </div>

            <form id="tcsForm" class="space-y-6">
              <!-- TCS tokens are handled server-side via /api/tcs proxies -->

              <!-- Access/Top level fields -->
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- <div class="text-sm text-gray-600">
                  TCS credentials are managed securely on the server.
                </div> -->
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Consignment No (optional)</label>
                  <input type="text" id="tcs_consignmentno" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-red-500">
                </div>
              </div>

              <!-- Shipper Info -->
              <div class="bg-gray-50 p-4 rounded-xl border">
                <h3 class="font-semibold text-gray-800 mb-4">Shipper Info</h3>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">TCS Account *</label>
                    <input type="text" id="tcs_shipper_tcsaccount" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Shipper Name *</label>
                    <input type="text" id="tcs_shipper_name" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Mobile (0300xxxxxxx) *</label>
                    <input type="text" id="tcs_shipper_mobile" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div class="md:col-span-3 grid grid-cols-1 md:grid-cols-3 gap-4">
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Address 1 *</label>
                      <input type="text" id="tcs_shipper_address1" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Address 2</label>
                      <input type="text" id="tcs_shipper_address2" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Address 3</label>
                      <input type="text" id="tcs_shipper_address3" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">ZIP</label>
                      <input type="text" id="tcs_shipper_zip" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Country Code (e.g., PK) *</label>
                      <input type="text" id="tcs_shipper_countrycode" class="w-full p-3 border border-gray-300 rounded-lg" value="PK">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Country Name *</label>
                      <input type="text" id="tcs_shipper_countryname" class="w-full p-3 border border-gray-300 rounded-lg" value="Pakistan">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">City Code (e.g., KHI)</label>
                      <input type="text" id="tcs_shipper_citycode" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">City Name *</label>
                      <select id="tcs_shipper_cityname" class="w-full p-3 border border-gray-300 rounded-lg">
                        <option value="">Loading cities...</option>
                      </select>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Consignee Info -->
              <div class="bg-gray-50 p-4 rounded-xl border">
                <h3 class="font-semibold text-gray-800 mb-4">Consignee Info</h3>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Consignee Code</label>
                    <input type="text" id="tcs_consignee_code" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">First Name *</label>
                    <input type="text" id="tcs_consignee_firstname" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Middle Name *</label>
                    <input type="text" id="tcs_consignee_middlename" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Last Name</label>
                    <input type="text" id="tcs_consignee_lastname" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>

                  <div class="md:col-span-3 grid grid-cols-1 md:grid-cols-3 gap-4">
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Address 1 *</label>
                      <input type="text" id="tcs_consignee_address1" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Address 2</label>
                      <input type="text" id="tcs_consignee_address2" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Address 3</label>
                      <input type="text" id="tcs_consignee_address3" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">ZIP</label>
                      <input type="text" id="tcs_consignee_zip" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Country Code (e.g., PK) *</label>
                      <input type="text" id="tcs_consignee_countrycode" class="w-full p-3 border border-gray-300 rounded-lg" value="PK">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Country Name *</label>
                      <input type="text" id="tcs_consignee_countryname" class="w-full p-3 border border-gray-300 rounded-lg" value="Pakistan">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">City Code</label>
                      <input type="text" id="tcs_consignee_citycode" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">City Name *</label>
                      <select id="tcs_consignee_cityname" class="w-full p-3 border border-gray-300 rounded-lg">
                        <option value="">Loading cities...</option>
                      </select>
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Email</label>
                      <input type="email" id="tcs_consignee_email" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Area Code</label>
                      <input type="text" id="tcs_consignee_areacode" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Area Name</label>
                      <input type="text" id="tcs_consignee_areaname" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Block Code</label>
                      <input type="text" id="tcs_consignee_blockcode" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Block Name</label>
                      <input type="text" id="tcs_consignee_blockname" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Latitude</label>
                      <input type="text" id="tcs_consignee_lat" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Longitude</label>
                      <input type="text" id="tcs_consignee_lng" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div>
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Landmark</label>
                      <input type="text" id="tcs_consignee_landmark" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                    <div class="md:col-span-3">
                      <label class="block text-sm font-semibold text-gray-700 mb-2">Mobile (0300xxxxxxx) *</label>
                      <input type="text" id="tcs_consignee_mobile" class="w-full p-3 border border-gray-300 rounded-lg">
                    </div>
                  </div>
                </div>
              </div>

              <!-- Vendor Info (optional) -->
              <div class="bg-gray-50 p-4 rounded-xl border">
                <h3 class="font-semibold text-gray-800 mb-4">Vendor Info (optional)</h3>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Name</label>
                    <input type="text" id="tcs_vendor_name" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Address 1</label>
                    <input type="text" id="tcs_vendor_address1" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Address 2</label>
                    <input type="text" id="tcs_vendor_address2" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Address 3</label>
                    <input type="text" id="tcs_vendor_address3" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">City Code</label>
                    <input type="text" id="tcs_vendor_citycode" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">City Name</label>
                    <select id="tcs_vendor_cityname" class="w-full p-3 border border-gray-300 rounded-lg">
                      <option value="">Loading cities...</option>
                    </select>
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Mobile</label>
                    <input type="text" id="tcs_vendor_mobile" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                </div>
              </div>

              <!-- Shipment Info -->
              <div class="bg-gray-50 p-4 rounded-xl border">
                <h3 class="font-semibold text-gray-800 mb-4">Shipment Info</h3>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Cost Center Code *</label>
                    <input type="text" id="tcs_ship_costcentercode" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-red-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Service Code *</label>
                    <input type="text" id="tcs_ship_servicecode" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-red-500" maxlength="6" minlength="1">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Reference No</label>
                    <input type="text" id="tcs_ship_referenceno" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Pieces *</label>
                    <input type="number" id="tcs_ship_pieces" class="w-full p-3 border border-gray-300 rounded-lg" min="1" step="1" value="1">
                  </div>
                  <div class="md:col-span-3">
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Content Description *</label>
                    <div class="relative">
                      <input type="text" id="tcs_ship_contentdesc" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-red-500" placeholder="Search for products..." autocomplete="off">
                      <input type="hidden" id="tcs_selected_product_id" value="">
                      <div id="tcs_product_dropdown" class="absolute z-10 w-full bg-white border border-gray-300 rounded-lg shadow-lg mt-1 hidden max-h-60 overflow-y-auto">
                        <!-- Product options will be populated here -->
                      </div>
                    </div>
                    <div id="tcs_stock_info" class="mt-2 text-sm text-gray-600 hidden">
                      <span id="tcs_stock_text"></span>
                    </div>
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Currency *</label>
                    <input type="text" id="tcs_ship_currency" class="w-full p-3 border border-gray-300 rounded-lg" value="PKR">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">COD Amount *</label>
                    <input type="number" id="tcs_ship_codamount" class="w-full p-3 border border-gray-300 rounded-lg" min="0" step="1">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Weight in KG *</label>
                    <input type="number" id="tcs_ship_weightinkg" class="w-full p-3 border border-gray-300 rounded-lg" min="0.5" step="0.1">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Fragile *</label>
                    <select id="tcs_ship_fragile" class="w-full p-3 border border-gray-300 rounded-lg">
                      <option value="false">No</option>
                      <option value="true">Yes</option>
                    </select>
                  </div>
                  <div class="md:col-span-3">
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Remarks</label>
                    <input type="text" id="tcs_ship_remarks" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                </div>
              </div>

              <!-- Single SKU (optional) -->
              <div class="bg-gray-50 p-4 rounded-xl border">
                <h3 class="font-semibold text-gray-800 mb-4">SKU (optional)</h3>
                <div class="grid grid-cols-1 md:grid-cols-5 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Description</label>
                    <input type="text" id="tcs_sku_description" class="w-full p-3 border border-gray-300 rounded-lg">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Quantity</label>
                    <input type="number" id="tcs_sku_quantity" class="w-full p-3 border border-gray-300 rounded-lg" min="1" step="1">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Weight (KG)</label>
                    <input type="number" id="tcs_sku_weight" class="w-full p-3 border border-gray-300 rounded-lg" min="0.5" step="0.1">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">UOM</label>
                    <input type="text" id="tcs_sku_uom" class="w-full p-3 border border-gray-300 rounded-lg" placeholder="KG">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Unit Price</label>
                    <input type="number" id="tcs_sku_unitprice" class="w-full p-3 border border-gray-300 rounded-lg" min="1" step="1">
                  </div>
                </div>
              </div>

              <div class="flex justify-end space-x-4 pt-6">
                <button type="button" onclick="closeTcsModal()"
                  class="px-6 py-3 bg-gray-300 hover:bg-gray-400 text-gray-800 rounded-lg font-semibold transition-colors">Cancel</button>
                <button type="submit"
                  class="px-6 py-3 bg-red-600 hover:bg-red-700 text-white rounded-lg font-semibold transition-colors">Create TCS Order</button>
              </div>
            </form>
          </div>
        </div>

        <!-- SEE MORE DETAILS MODAL -->
        <div id="seeMoreModal" class="fixed inset-0 bg-black/50 z-50 hidden flex items-center justify-center">
          <div id="seeMoreContent"
            class="bg-white rounded-xl w-full max-w-lg shadow-xl transform scale-95 opacity-0 transition-all duration-300">
            <!-- Modal header -->
            <div
              class="bg-gradient-to-r from-green-600 to-indigo-600 text-white rounded-t-xl px-6 py-4 flex justify-between items-center">
              <h2 class="text-xl font-bold"><i class="fas fa-info-circle mr-2"></i>Order Details</h2>
              <button onclick="closeSeeMoreModal()" class="text-white hover:text-gray-200 transition">
                <i class="fas fa-times"></i>
              </button>
            </div>
            <!-- Modal body -->
            <div class="p-6">
              <ul id="detailsList" class="space-y-3 text-gray-700 text-sm"></ul>
            </div>
            <!-- Modal footer -->
          </div>
        </div>
        <!-- Return Modal -->
        <div id="returnModal" class="fixed inset-0 bg-black bg-opacity-40 hidden items-center justify-center z-50">
          <div class="bg-white rounded-2xl p-8 w-full max-w-md shadow-xl">
            <h2 class="text-xl font-bold text-gray-800 mb-4">Mark as Returned</h2>
            <p class="text-gray-600 mb-6">Are you sure you want to mark this order as returned?
              This will update the tracking ID and restore the quantity back to stock.</p>
            <div class="flex justify-end space-x-4">
              <button onclick="closeReturnModal()"
                class="bg-gray-300 hover:bg-gray-400 text-gray-800 px-4 py-2 rounded-lg">Cancel</button>
              <form id="returnForm" method="POST">
                <button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg">Yes,
                  Return</button>
              </form>
            </div>
          </div>
        </div>

        <!-- Leopards Courier Booking Modal -->
        <div id="leopardsModal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
          <div class="bg-white rounded-2xl p-8 w-full max-w-4xl max-h-[90vh] overflow-y-auto shadow-xl">
            <div class="flex justify-between items-center mb-6">
              <h2 class="text-2xl font-bold text-gray-800 flex items-center">
                <i class="fas fa-truck mr-3 text-green-600"></i>
                Leopards Courier Booking
              </h2>
              <button onclick="closeLeopardsModal()" class="text-gray-500 hover:text-gray-700 text-2xl">
                <i class="fas fa-times"></i>
              </button>
            </div>

            <form id="leopardsForm" class="space-y-6">
              <!-- API Credentials (Hidden) -->
              <input type="hidden" id="api_key" value="c7ab1d5f6b35f23b496c9cc24d4715c1">
              <input type="hidden" id="api_password" value="123456">

              <!-- Packet Details -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="text-lg font-semibold text-gray-800 mb-4">Packet Details</h3>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Weight (Grams) *</label>
                    <input type="number" id="booked_packet_weight" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">No. of Pieces *</label>
                    <input type="number" id="booked_packet_no_piece" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Collection Amount</label>
                    <input type="number" id="booked_packet_collect_amount"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Order ID</label>
                    <input type="text" id="booked_packet_order_id"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Shipment ID</label>
                    <input type="number" id="shipment_id" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                </div>
              </div>

              <!-- Volumetric Weight (Optional) -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="text-lg font-semibold text-gray-800 mb-4">Volumetric Weight (Optional)</h3>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Width</label>
                    <input type="number" id="booked_packet_vol_weight_w"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Height</label>
                    <input type="number" id="booked_packet_vol_weight_h"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Length</label>
                    <input type="number" id="booked_packet_vol_weight_l"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                </div>
              </div>

              <!-- Cities -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <div class="mb-4">
                  <h3 class="text-lg font-semibold text-gray-800">Cities</h3>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Origin City *</label>
                    <select id="origin_city" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                      <option value="">Select Origin City</option>
                      <option value="self">Self (Your City)</option>
                    </select>
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Destination City *</label>
                    <select id="destination_city" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                      <option value="">Select Destination City</option>
                      <option value="self">Self (Your City)</option>
                    </select>
                  </div>
                </div>
              </div>

              <!-- Shipper Details -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="text-lg font-semibold text-gray-800 mb-4">Shipper Details</h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Name</label>
                    <input type="text" id="shipment_name_eng" value="self"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Email</label>
                    <input type="email" id="shipment_email" value="self"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Phone</label>
                    <input type="tel" id="shipment_phone" value="self"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Address</label>
                    <input type="text" id="shipment_address" value="self"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                </div>
              </div>

              <!-- Consignee Details -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="text-lg font-semibold text-gray-800 mb-4">Consignee Details</h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Name *</label>
                    <input type="text" id="consignment_name_eng" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Email</label>
                    <input type="email" id="consignment_email"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Phone *</label>
                    <input type="tel" id="consignment_phone" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Phone 2</label>
                    <input type="tel" id="consignment_phone_two"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Phone 3</label>
                    <input type="tel" id="consignment_phone_three"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Address *</label>
                    <input type="text" id="consignment_address" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                </div>
              </div>

              <!-- Additional Details -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="text-lg font-semibold text-gray-800 mb-4">Additional Details</h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Special Instructions</label>
                    <textarea id="special_instructions" rows="3"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500"></textarea>
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Shipment Type</label>
                    <input type="text" id="shipment_type" value="overnight"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Return Address</label>
                    <input type="text" id="return_address"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Return City</label>
                    <input type="number" id="return_city"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Is VPC</label>
                    <select id="is_vpc"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                      <option value="0">No</option>
                      <option value="1">Yes</option>
                    </select>
                  </div>
                </div>
              </div>

              <!-- Submit Button -->
              <div class="flex justify-end space-x-4 pt-6">
                <button type="button" onclick="closeLeopardsModal()"
                  class="px-6 py-3 bg-gray-300 hover:bg-gray-400 text-gray-800 rounded-lg font-semibold transition-colors">
                  Cancel
                </button>
                <button type="submit"
                  class="px-6 py-3 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white rounded-lg font-semibold shadow-lg transform hover:scale-105 transition-all duration-200">
                  <i class="fas fa-paper-plane mr-2"></i>
                  Book Package
                </button>
              </div>
            </form>
          </div>
        </div>

        <!-- M&P Courier Booking Modal -->
        <div id="mnpModal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
          <div class="bg-white rounded-2xl p-8 w-full max-w-3xl max-h-[90vh] overflow-y-auto shadow-xl">
            <div class="flex justify-between items-center mb-6">
              <h2 class="text-2xl font-bold text-gray-800 flex items-center">
                <i class="fas fa-shipping-fast mr-3 text-blue-600"></i>
                M&P Parcel Booking
              </h2>
              <button onclick="closeMnpModal()" class="text-gray-500 hover:text-gray-700 text-2xl">
                <i class="fas fa-times"></i>
              </button>
            </div>

            <form id="mnpForm" class="space-y-6">
              <!-- Consignee Details -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="text-lg font-semibold text-gray-800 mb-4">Consignee Details</h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Consignee Name *</label>
                    <input type="text" id="consigneeName" value="self" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Consignee Email</label>
                    <input type="email" id="consigneeEmail" value="example@example.com"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div class="md:col-span-2">
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Consignee Address *</label>
                    <input type="text" id="consigneeAddress" value="adfa" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Consignee Mobile No *</label>
                    <input type="text" id="consigneeMobNo" value="1234567890" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  
                </div>
              </div>

              <!-- Shipment Details -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="text-lg font-semibold text-gray-800 mb-4">Shipment Details</h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div class="md:col-span-2">
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Destination City Name *</label>
                    <select id="destinationCityName" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-black">
                      <option value="">Loading cities...</option>
                    </select>
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Weight (kg) *</label>
                    <input type="number" id="weight" value="1" min="0" step="0.01" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">COD Amount *</label>
                    <input type="number" id="codAmount" value="5" min="0" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Customer Reference No *</label>
                    <input type="text" id="custRefNo" value="T-786" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Pieces *</label>
                    <input type="number" id="pieces" value="1" min="1" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div class="md:col-span-2">
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Product Details *</label>
                    <div class="relative">
                      <input type="text" id="productDetails" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" placeholder="Search for products..." autocomplete="off">
                      <input type="hidden" id="mnp_selected_product_id" value="">
                      <div id="mnp_product_dropdown" class="absolute z-10 w-full bg-white border border-gray-300 rounded-lg shadow-lg mt-1 hidden max-h-60 overflow-y-auto">
                        <!-- Product options will be populated here -->
                      </div>
                    </div>
                    <div id="mnp_stock_info" class="mt-2 text-sm text-gray-600 hidden">
                      <span id="mnp_stock_text"></span>
                    </div>
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Fragile</label>
                    <select id="fragile"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                      <option value="Y">Yes</option>
                      <option value="N" selected>No</option>
                    </select>
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Service *</label>
                    <select id="service" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                      <option value="O" selected>Overnight</option>
                      <option value="S">Same Day</option>
                    </select>
                  </div>
                  <div class="md:col-span-2">
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Remarks</label>
                    <input type="text" id="remarks" value="q"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                </div>
              </div>

              <!-- Additional Options -->
              <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="text-lg font-semibold text-gray-800 mb-4">Additional Options</h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Insurance Value</label>
                    <input type="number" id="insuranceValue" value="0" min="0"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Location ID *</label>
                    <input type="number" id="locationID" value="1" min="0" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Insert Type</label>
                    <input type="text" id="InsertType" value="New"
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Return Location *</label>
                    <input type="text" id="ReturnLocation" value="Karachi" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Sub Account ID *</label>
                    <input type="text" id="subAccountId" value="Sub-123" required
                      class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                  </div>
                </div>
              </div>

              <div class="flex justify-end space-x-4 pt-4">
                <button type="button" onclick="closeMnpModal()"
                  class="px-6 py-3 bg-gray-300 hover:bg-gray-400 text-gray-800 rounded-lg font-semibold transition-colors">Cancel</button>
                <button type="submit"
                  class="px-6 py-3 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-lg font-semibold shadow-lg transform hover:scale-105 transition-all duration-200">
                  <i class="fas fa-paper-plane mr-2"></i>
                  Book Parcel
                </button>
              </div>
            </form>

            <!-- Response box removed as per request: show only success message on booking -->
          </div>
      </div>

      <!-- PostEx Courier Booking Modal -->
      <div id="postexModal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
        <div class="bg-white rounded-2xl p-8 w-full max-w-3xl max-h-[90vh] overflow-y-auto shadow-xl">
          <div class="flex justify-between items-center mb-6">
            <h2 class="text-2xl font-bold text-gray-800 flex items-center">
              <i class="fas fa-plane mr-3 text-purple-600"></i>
              PostEx Order Creation
            </h2>
            <button onclick="closePostexModal()" class="text-gray-500 hover:text-gray-700 text-2xl">
              <i class="fas fa-times"></i>
            </button>
          </div>

          <form id="postexForm" class="space-y-6">
            <!-- Hidden: API token (kept in JS as well) -->
            <input type="hidden" id="postex_token" value="YzIxZGU5YjY0N2M0NDU1ZGE0ZDM5ZmVlOTA3ZWEwODc6NGI4OTRjNzcyNjRkNDA1NTk3ZDNjODQwNDhhYmQ1Mjg=">

            <!-- Customer & Order Details -->
            <div class="bg-gray-50 p-4 rounded-lg">
              <h3 class="text-lg font-semibold text-gray-800 mb-4">Customer & Order Details</h3>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Order Ref Number *</label>
                  <input type="text" id="orderRefNumber" required class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Invoice Payment (Amount) *</label>
                  <input type="number" id="invoicePayment" required min="0" step="0.01" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
                <div class="md:col-span-2">
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Order Detail *</label>
                  <div class="relative">
                    <input type="text" id="orderDetail" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500" placeholder="Search for products..." autocomplete="off">
                    <input type="hidden" id="postex_selected_product_id" value="">
                    <div id="postex_product_dropdown" class="absolute z-10 w-full bg-white border border-gray-300 rounded-lg shadow-lg mt-1 hidden max-h-60 overflow-y-auto">
                      <!-- Product options will be populated here -->
                    </div>
                  </div>
                  <div id="postex_stock_info" class="mt-2 text-sm text-gray-600 hidden">
                    <span id="postex_stock_text"></span>
                  </div>
                </div>
              </div>
            </div>

            <!-- Consignee -->
            <div class="bg-gray-50 p-4 rounded-lg">
              <h3 class="text-lg font-semibold text-gray-800 mb-4">Consignee</h3>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Customer Name *</label>
                  <input type="text" id="customerName" required class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Customer Phone (03xxxxxxxxx) *</label>
                  <input type="text" id="customerPhone" required class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500" placeholder="03xxxxxxxxx">
                </div>
                <div class="md:col-span-2">
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Delivery Address *</label>
                  <input type="text" id="deliveryAddress" required class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
              </div>
            </div>

            <!-- Shipment -->
            <div class="bg-gray-50 p-4 rounded-lg">
              <h3 class="text-lg font-semibold text-gray-800 mb-4">Shipment</h3>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div class="md:col-span-2">
                  <label class="block text-sm font-semibold text-gray-700 mb-2">City Name *</label>
                  <select id="cityName" required class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 text-black">
                    <option value="">Loading cities...</option>
                  </select>
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Invoice Division *</label>
                  <input type="number" id="invoiceDivision" min="0" required class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Items (Pieces) *</label>
                  <input type="number" id="items" min="1" required class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Order Type *</label>
                  <select id="orderType" required class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                    <option value="Normal" selected>Normal</option>
                    <option value="Reverse">Reverse</option>
                    <option value="Replacement">Replacement</option>
                  </select>
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Transaction Notes</label>
                  <input type="text" id="transactionNotes" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Pickup Address Code</label>
                  <input type="text" id="pickupAddressCode" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Store Address Code</label>
                  <input type="text" id="storeAddressCode" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
              </div>
            </div>

            <div class="flex justify-end space-x-4 pt-4">
              <button type="button" onclick="closePostexModal()" class="px-6 py-3 bg-gray-300 hover:bg-gray-400 text-gray-800 rounded-lg font-semibold transition-colors">Cancel</button>
              <button type="submit" class="px-6 py-3 bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white rounded-lg font-semibold shadow-lg transform hover:scale-105 transition-all duration-200">
                <i class="fas fa-paper-plane mr-2"></i>
                Create Order
              </button>
            </div>
          </form>
        </div>
      </div>

      <!-- Toast Container -->
      <div id="toastContainer" class="fixed top-4 right-4 z-50 space-y-2"></div>

      <script>
          // Toast helper
          function showToast(message, type = 'success') {
            const container = document.getElementById('toastContainer');
            if (!container) return;

            const color = type === 'success' ? 'bg-green-600' : type === 'error' ? 'bg-red-600' : 'bg-gray-700';
            const border = type === 'success' ? 'border-green-700' : type === 'error' ? 'border-red-700' : 'border-gray-800';

            const toast = document.createElement('div');
            toast.className = `${color} ${border} text-white px-4 py-3 rounded shadow-lg border flex items-start gap-3 transition-opacity duration-300`;
            toast.innerHTML = `
              <span class="mt-0.5">
                ${type === 'success' ? '✅' : type === 'error' ? '❌' : 'ℹ️'}
              </span>
              <div class="text-sm leading-5">${message}</div>
            `;
            container.appendChild(toast);

            // Auto dismiss after 4s
            setTimeout(() => {
              toast.classList.add('opacity-0');
              setTimeout(() => toast.remove(), 300);
            }, 4000);
          }
          // Product selection and stock management
          const prodSelect = document.getElementById('productId');
          const stockField = document.getElementById('availableStock');
          const qtyInput = document.getElementById('quantity');

          if (prodSelect) {
            prodSelect.addEventListener('change', () => {
              const opt = prodSelect.selectedOptions[0];
              const st = opt.dataset.stock;
              stockField.value = st || '—';
              qtyInput.max = st || '';
              if (qtyInput.value > st) qtyInput.value = st || 1;
            });
          }
          // Delivery vs Office pickup validation
          const pickupMethodSelect = document.getElementById('pickupMethod');
          const deliveryFields = document.getElementById('deliveryFields');
          const serviceSelect = document.getElementById('service');
          const addressInput = document.getElementById('address');
          const postalCodeInput = document.getElementById('postalCode');
          const cityInput = document.getElementById('city');

          function updateDeliveryFields() {
            if (pickupMethodSelect.value === 'delivery') {
              deliveryFields.style.display = 'grid';
              serviceSelect.required = true;
              addressInput.required = true;
              postalCodeInput.required = true;
              cityInput.required = true;
              serviceSelect.disabled = false;
              addressInput.disabled = false;
              postalCodeInput.disabled = false;
              cityInput.disabled = false;
            } else {
              deliveryFields.style.display = 'none';
              serviceSelect.required = false;
              addressInput.required = false;
              postalCodeInput.required = false;
              cityInput.required = false;
              serviceSelect.value = '';
              addressInput.value = '';
              postalCodeInput.value = '';
              cityInput.value = '';
              serviceSelect.disabled = true;
              addressInput.disabled = true;
              postalCodeInput.disabled = true;
              cityInput.disabled = true;
            }
          }
          if (pickupMethodSelect) {
            pickupMethodSelect.addEventListener('change', updateDeliveryFields);
            updateDeliveryFields();
          }
          // ------------------------
          let currentOrderId = null;

          function openTrackingModal(orderId) {
            currentOrderId = orderId;
            document.getElementById('trackingIdModal').classList.remove('hidden');
          }

          function closeTrackingModal() {
            document.getElementById('trackingIdModal').classList.add('hidden');
            currentOrderId = null;
            document.getElementById('trackingInputField').value = '';
          }
          async function saveTrackingId() {
            const trackingId = document.getElementById('trackingInputField').value.trim();
            if (!trackingId || !currentOrderId) return alert('Please enter a valid Tracking ID');

            try {
              const res = await fetch('/admin/create-order/update-tracking', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ orderId: currentOrderId, trackingId })
              });
              const data = await res.json();
              if (data.success) {
                location.reload();
              } else {
                alert(data.message || 'Error updating tracking ID');
              }
            } catch (err) {
              console.error(err);
              alert('Failed to update tracking ID');
            }
          }
          // ------------------------
          function showMoreDetails(btn) {
            const order = JSON.parse(btn.getAttribute('data-order'));
            const detailsList = document.getElementById('detailsList');

            detailsList.innerHTML = `
      <li class="py-2 flex items-start"><i class="fas fa-envelope mr-2 text-green-500"></i><strong>Name:</strong> <span class="ml-1">${order.customerName || '-'}</span></li>
      <li class="py-2 flex items-start"><i class="fas fa-envelope mr-2 text-green-500"></i><strong>Email:</strong> <span class="ml-1">${order.email || '-'}</span></li>
      <li class="py-2 flex items-start"><i class="fas fa-map-marker-alt mr-2 text-red-500"></i><strong>Address:</strong> <span class="ml-1">${order.address || '-'}</span></li>
      <li class="py-2 flex items-start"><i class="fas fa-mail-bulk mr-2 text-indigo-500"></i><strong>Postal Code:</strong> <span class="ml-1">${order.postalCode || '-'}</span></li>
      <li class="py-2 flex items-start"><i class="fas fa-city mr-2 text-orange-500"></i><strong>City:</strong> <span class="ml-1">${order.city || '-'}</span></li>
      <li class="py-2 flex items-start"><i class="fas fa-calendar-alt mr-2 text-gray-500"></i><strong>Created At:</strong> <span class="ml-1">${new Date(order.createdAt).toLocaleString()}</span></li>
    `;
            const modal = document.getElementById('seeMoreModal');
            const content = document.getElementById('seeMoreContent');
            modal.classList.remove('hidden');
            setTimeout(() => {
              content.classList.remove('scale-95', 'opacity-0');
            }, 20);
          }
          function closeSeeMoreModal() {
            const content = document.getElementById('seeMoreContent');
            content.classList.add('scale-95', 'opacity-0');
            setTimeout(() => {
              document.getElementById('seeMoreModal').classList.add('hidden');
            }, 300);
          }
          function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            const texts = sidebar.querySelectorAll('.sidebar-text');
            const profilePic = sidebar.querySelector('.profile-pic-container');

            sidebar.classList.toggle('w-64');
            sidebar.classList.toggle('w-20');

            texts.forEach(el => el.classList.toggle('hidden'));

            profilePic.classList.toggle('w-24');
            profilePic.classList.toggle('h-24');
            profilePic.classList.toggle('w-12');
            profilePic.classList.toggle('h-12');

            // also adjust p-6 vs p-2 padding on sidebar
            sidebar.classList.toggle('p-6');
            sidebar.classList.toggle('p-2');
          }



          function openReturnModal(orderId) {
            const form = document.getElementById('returnForm');
            form.action = `/admin/create-order/return/${orderId}`;
            document.getElementById('returnModal').classList.remove('hidden');
            document.getElementById('returnModal').classList.add('flex');
          }

          function closeReturnModal() {
            document.getElementById('returnModal').classList.remove('flex');
            document.getElementById('returnModal').classList.add('hidden');
          }

          // Leopards Modal Functions
          function openLeopardsModal() {
            document.getElementById('leopardsModal').classList.remove('hidden');
            document.getElementById('leopardsModal').classList.add('flex');

            // Load cities with a small delay to ensure modal is fully rendered
            setTimeout(() => {
              loadCities();
            }, 100);
          }

          function closeLeopardsModal() {
            document.getElementById('leopardsModal').classList.remove('flex');
            document.getElementById('leopardsModal').classList.add('hidden');
          }

          // Load cities from Leopards API
          async function loadCities() {
            try {
              console.log('Loading cities...');
              const response = await fetch('https://merchantapistaging.leopardscourier.com/api/getAllCities/format/json/', {
                method: 'POST',
                headers: {
                  'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                  api_key: 'c7ab1d5f6b35f23b496c9cc24d4715c1',
                  api_password: '123456'
                })
              });

              console.log('Response status:', response.status);
              const data = await response.json();
              console.log('API Response:', data);

              if (data.status === 1 && data.city_list) {
                console.log('Cities found:', data.city_list.length);
                populateCityDropdowns(data.city_list);
              } else {
                console.error('No cities found in response:', data);
              }
            } catch (error) {
              console.error('Error loading cities:', error);
            }
          }

          // Populate city dropdowns
          function populateCityDropdowns(cities) {
            console.log('Populating city dropdowns with:', cities);

            if (cities.length > 0) {
              console.log('First city object structure:', cities[0]);
            }

            const originSelect = document.getElementById('origin_city');
            const destSelect = document.getElementById('destination_city');

            if (!originSelect || !destSelect) {
              console.error('City dropdown elements not found!');
              return;
            }

            console.log('Found dropdown elements:', { originSelect, destSelect });

            // Clear existing options except "self"
            originSelect.innerHTML = '<option value="">Select Origin City</option><option value="self">Self (Your City)</option>';
            destSelect.innerHTML = '<option value="">Select Destination City</option><option value="self">Self (Your City)</option>';

            cities.forEach(city => {
              console.log('Adding city:', city);

              // Try different possible field names for city ID and name
              const cityId = city.city_id || city.id || city.cityId || city.cityid;
              const cityName = city.city_name || city.name || city.cityName || city.cityname;

              if (cityId && cityName) {
                const originOption = document.createElement('option');
                originOption.value = cityId;
                originOption.textContent = cityName;
                originSelect.appendChild(originOption);

                const destOption = document.createElement('option');
                destOption.value = cityId;
                destOption.textContent = cityName;
                destSelect.appendChild(destOption);
              } else {
                console.warn('City object missing required fields:', city);
              }
            });

            console.log('City dropdowns populated. Origin options:', originSelect.options.length, 'Destination options:', destSelect.options.length);
          }

          // M&P Modal Functions
          function openMnpModal() {
            document.getElementById('mnpModal').classList.remove('hidden');
            document.getElementById('mnpModal').classList.add('flex');
            setTimeout(() => {
              loadMnpCities();
            }, 100);
          }

          function closeMnpModal() {
            document.getElementById('mnpModal').classList.remove('flex');
            document.getElementById('mnpModal').classList.add('hidden');
            
            // Reset form and clear product selection
            const form = document.getElementById('mnpForm');
            if (form) form.reset();
            
            // Clear product selection
            const productIdInput = document.getElementById('mnp_selected_product_id');
            const stockInfo = document.getElementById('mnp_stock_info');
            const dropdown = document.getElementById('mnp_product_dropdown');
            
            if (productIdInput) productIdInput.value = '';
            if (stockInfo) stockInfo.classList.add('hidden');
            if (dropdown) dropdown.classList.add('hidden');
          }

          // Product search functionality for M&P Product Details
          (function initMnpProductSearch() {
            const searchInput = document.getElementById('productDetails');
            const dropdown = document.getElementById('mnp_product_dropdown');
            const productIdInput = document.getElementById('mnp_selected_product_id');
            const stockInfo = document.getElementById('mnp_stock_info');
            const stockText = document.getElementById('mnp_stock_text');
            const piecesInput = document.getElementById('pieces');
            
            if (!searchInput || !dropdown || !productIdInput) return;
            
            let searchTimeout;
            let selectedProduct = null;
            
            // Search products as user types
            searchInput.addEventListener('input', function() {
              const query = this.value.trim();
              
              clearTimeout(searchTimeout);
              
              if (query.length < 1) {
                dropdown.classList.add('hidden');
                productIdInput.value = '';
                selectedProduct = null;
                stockInfo.classList.add('hidden');
                return;
              }
              
              searchTimeout = setTimeout(async () => {
                try {
                  const response = await fetch(`/api/products/search?q=${encodeURIComponent(query)}`);
                  const products = await response.json();
                  
                  dropdown.innerHTML = '';
                  
                  if (products.length === 0) {
                    dropdown.innerHTML = '<div class="p-3 text-gray-500">No products found</div>';
                  } else {
                    products.forEach(product => {
                      const div = document.createElement('div');
                      div.className = 'p-3 hover:bg-gray-100 cursor-pointer border-b border-gray-100 last:border-b-0';
                      div.innerHTML = `
                        <div class="font-medium">${product.name}</div>
                        <div class="text-sm text-gray-600">Stock: ${product.stock} available</div>
                      `;
                      div.addEventListener('click', () => selectProduct(product));
                      dropdown.appendChild(div);
                    });
                  }
                  
                  dropdown.classList.remove('hidden');
                } catch (error) {
                  console.error('Product search error:', error);
                  dropdown.innerHTML = '<div class="p-3 text-red-500">Error searching products</div>';
                  dropdown.classList.remove('hidden');
                }
              }, 300);
            });
            
            // Select a product
            function selectProduct(product) {
              selectedProduct = product;
              searchInput.value = product.name;
              productIdInput.value = product._id;
              dropdown.classList.add('hidden');
              
              // Show stock information
              stockText.textContent = `Available stock: ${product.stock}`;
              stockInfo.classList.remove('hidden');
              
              // Validate pieces against stock
              validateStock();
            }
            
            // Validate stock when pieces change
            if (piecesInput) {
              piecesInput.addEventListener('input', validateStock);
            }
            
            function validateStock() {
              if (!selectedProduct || !piecesInput) return;
              
              const pieces = parseInt(piecesInput.value) || 0;
              const available = selectedProduct.stock;
              
              if (pieces > available) {
                stockText.textContent = `Insufficient stock! Available: ${available}, Requested: ${pieces}`;
                stockText.className = 'text-red-600';
                piecesInput.classList.add('border-red-500');
              } else {
                stockText.textContent = `Available stock: ${available}`;
                stockText.className = 'text-green-600';
                piecesInput.classList.remove('border-red-500');
              }
            }
            
            // Hide dropdown when clicking outside
            document.addEventListener('click', function(e) {
              if (!searchInput.contains(e.target) && !dropdown.contains(e.target)) {
                dropdown.classList.add('hidden');
              }
            });
          })();

          // Load Cities into dropdown (M&P)
          async function loadMnpCities() {
            const username = "ALI.RAZA_18O59";
            const password = "AliRaza@123";
            const accountNo = "4T1542";
            const locationID = "29609";

            const url = `http://mnpcourier.com/mycodapi/api/Branches/Get_Cities?username=${username}&password=${password}&accountNo=${accountNo}&locationID=${locationID}`;

            try {
              const response = await fetch(url);
              const cities = await response.json();

              const dropdown = document.getElementById("destinationCityName");
              dropdown.innerHTML = `<option value="">-- Select City --</option>`;

              // API seems to return array, derive city names accordingly
              // Try cities[0].City as user’s snippet suggests it contains array of names
              let cityList = [];
              if (Array.isArray(cities)) {
                if (Array.isArray(cities[0]?.City)) {
                  cityList = cities[0].City;
                } else {
                  // Fallback: assume each item has City property as string
                  cityList = cities.map(c => c.City || c.city || c.name).filter(Boolean);
                }
              }

              cityList.forEach(city => {
                const option = document.createElement("option");
                option.value = city;
                option.textContent = city;
                dropdown.appendChild(option);
              });
            } catch (err) {
              console.error("Error loading cities:", err);
              document.getElementById("destinationCityName").innerHTML = `<option value="">Failed to load cities</option>`;
            }
          }

          // Booking Form Submission (M&P)
          document.getElementById("mnpForm").addEventListener("submit", async function (e) {
            e.preventDefault();

            const username = "ALI.RAZA_18O59";
            const password = "AliRaza@123";
            const accountNo = "4T1542";

            // helper to safely read optional fields
            const getVal = (id) => {
              const el = document.getElementById(id);
              return el ? el.value : '';
            };

            // Validate product selection and stock
            const productId = getVal('mnp_selected_product_id');
            const pieces = parseInt(getVal('pieces')) || 0;
            
            if (!productId) {
              if (typeof showToast === 'function') showToast('Please select a product from the dropdown', 'error');
              return;
            }

            const formData = {
              consigneeName: getVal('consigneeName'),
              consigneeAddress: getVal('consigneeAddress'),
              consigneeMobNo: getVal('consigneeMobNo'),
              // consigneeCell removed from form; send empty if not required by API
              consigneeCell: '',
              consigneeEmail: getVal('consigneeEmail'),
              destinationCityName: getVal('destinationCityName'),
              pieces: pieces,
              weight: getVal('weight'),
              codAmount: getVal('codAmount'),
              custRefNo: getVal('custRefNo'),
              productDetails: getVal('productDetails'),
              fragile: getVal('fragile'),
              service: getVal('service'),
              remarks: getVal('remarks'),
              insuranceValue: getVal('insuranceValue'),
              locationID: getVal('locationID'),
              InsertType: getVal('InsertType'),
              ReturnLocation: getVal('ReturnLocation'),
              subAccountId: getVal('subAccountId'),
              username,
              password,
              accountNo,
              // Add product info for backend processing
              productId: productId,
              piecesCount: pieces
            };

            const url = `/api/mnp/booking`;

            try {
              const response = await fetch(url, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify(formData)
              });

              const data = await response.json();

              // Interpret success based on API shape (array or object)
              let success = false;
              let message = '';
              let ref = '';

              if (Array.isArray(data)) {
                const item = data[0] || {};
                success = item.isSuccess === true || item.isSuccess === 'true' || item.success === true || response.ok;
                message = item.message || 'Order saved successfully!';
                ref = item.orderReferenceId || item.consignment || item.trackingNo || '';
              } else if (data && typeof data === 'object') {
                success = data.isSuccess === true || data.isSuccess === 'true' || data.success === true || response.ok;
                message = data.message || 'Order saved successfully!';
                ref = data.orderReferenceId || data.consignment || data.trackingNo || '';
              } else {
                success = response.ok;
              }

              if (success) {
                showToast(`${message}${ref ? ` (Ref: ${ref})` : ''}`, 'success');
                closeMnpModal(); // This will also reset the form
              } else {
                showToast(`Booking failed${message ? `: ${message}` : ''}`,'error');
              }
            } catch (err) {
              console.error("Booking Error:", err);
              showToast("Booking Error: " + err.message, 'error');
            }
          });

          // PostEx Modal + API Integration
          // Expose open/close on window so inline onclick can access them
          window.openPostexModal = function () {
            const modal = document.getElementById('postexModal');
            if (!modal) return;
            modal.classList.remove('hidden');
            modal.classList.add('flex');
            // If city selected matches a known address code and inputs are empty, prefill storeAddressCode
            try {
              const city = (document.getElementById('cityName')?.value || '').trim().toUpperCase();
              const knownCodes = { 'MULTAN': '001' };
              if (city && knownCodes[city]) {
                const storeEl = document.getElementById('storeAddressCode');
                const pickupEl = document.getElementById('pickupAddressCode');
                if (storeEl && !storeEl.value) storeEl.value = knownCodes[city];
                if (pickupEl && !pickupEl.value) pickupEl.placeholder = `Try ${knownCodes[city]} if needed`;
              }
            } catch(_) {}
            setTimeout(() => {
              loadPostexCities();
              // Initialize product search when modal opens
              try {
                initPostexProductSearchOnOpen();
              } catch (error) {
                console.error('Error initializing PostEx product search:', error);
              }
            }, 100);
          }
          window.closePostexModal = function () {
            const modal = document.getElementById('postexModal');
            if (!modal) return;
            modal.classList.remove('flex');
            modal.classList.add('hidden');
            
            // Reset form and clear product selection
            const form = document.getElementById('postexForm');
            if (form) form.reset();
            
            // Clear product selection
            const productIdInput = document.getElementById('postex_selected_product_id');
            const stockInfo = document.getElementById('postex_stock_info');
            const dropdown = document.getElementById('postex_product_dropdown');
            
            if (productIdInput) productIdInput.value = '';
            if (stockInfo) stockInfo.classList.add('hidden');
            if (dropdown) dropdown.classList.add('hidden');
          }

          // Product search functionality for PostEx Order Detail
          let postexProductSearchInitialized = false;
          
          function initPostexProductSearchOnOpen() {
            if (postexProductSearchInitialized) return;
            
            const searchInput = document.getElementById('orderDetail');
            const dropdown = document.getElementById('postex_product_dropdown');
            const productIdInput = document.getElementById('postex_selected_product_id');
            const stockInfo = document.getElementById('postex_stock_info');
            const stockText = document.getElementById('postex_stock_text');
            const itemsInput = document.getElementById('items');
            
            console.log('PostEx Product Search - Elements found:', {
              searchInput: !!searchInput,
              dropdown: !!dropdown,
              productIdInput: !!productIdInput,
              stockInfo: !!stockInfo,
              stockText: !!stockText,
              itemsInput: !!itemsInput
            });
            
            if (!searchInput || !dropdown || !productIdInput) {
              console.error('PostEx Product Search - Missing required elements');
              return;
            }
            
            let searchTimeout;
            let selectedProduct = null;
            
            // Search products as user types
            searchInput.addEventListener('input', function() {
              const query = this.value.trim();
              
              clearTimeout(searchTimeout);
              
              if (query.length < 1) {
                dropdown.classList.add('hidden');
                productIdInput.value = '';
                selectedProduct = null;
                stockInfo.classList.add('hidden');
                return;
              }
              
              searchTimeout = setTimeout(async () => {
                try {
                  const response = await fetch(`/api/products/search?q=${encodeURIComponent(query)}`);
                  const products = await response.json();
                  
                  dropdown.innerHTML = '';
                  
                  if (products.length === 0) {
                    dropdown.innerHTML = '<div class="p-3 text-gray-500">No products found</div>';
                  } else {
                    products.forEach(product => {
                      const div = document.createElement('div');
                      div.className = 'p-3 hover:bg-gray-100 cursor-pointer border-b border-gray-100 last:border-b-0';
                      div.innerHTML = `
                        <div class="font-medium">${product.name}</div>
                        <div class="text-sm text-gray-600">Stock: ${product.stock} available</div>
                      `;
                      div.addEventListener('click', () => selectProduct(product));
                      dropdown.appendChild(div);
                    });
                  }
                  
                  dropdown.classList.remove('hidden');
                } catch (error) {
                  console.error('Product search error:', error);
                  dropdown.innerHTML = '<div class="p-3 text-red-500">Error searching products</div>';
                  dropdown.classList.remove('hidden');
                }
              }, 300);
            });
            
            // Select a product
            function selectProduct(product) {
              selectedProduct = product;
              searchInput.value = product.name;
              productIdInput.value = product._id;
              dropdown.classList.add('hidden');
              
              // Show stock information
              stockText.textContent = `Available stock: ${product.stock}`;
              stockInfo.classList.remove('hidden');
              
              // Validate items against stock
              validateStock();
            }
            
            // Validate stock when items change
            if (itemsInput) {
              itemsInput.addEventListener('input', validateStock);
            }
            
            function validateStock() {
              if (!selectedProduct || !itemsInput) return;
              
              const items = parseInt(itemsInput.value) || 0;
              const available = selectedProduct.stock;
              
              if (items > available) {
                stockText.textContent = `Insufficient stock! Available: ${available}, Requested: ${items}`;
                stockText.className = 'text-red-600';
                itemsInput.classList.add('border-red-500');
              } else {
                stockText.textContent = `Available stock: ${available}`;
                stockText.className = 'text-green-600';
                itemsInput.classList.remove('border-red-500');
              }
            }
            
            // Hide dropdown when clicking outside
            document.addEventListener('click', function(e) {
              if (!searchInput.contains(e.target) && !dropdown.contains(e.target)) {
                dropdown.classList.add('hidden');
              }
            });
            
            postexProductSearchInitialized = true;
            console.log('PostEx Product Search - Initialized successfully');
          }

          async function loadPostexCities() {
            const dropdown = document.getElementById('cityName');
            if (!dropdown) return;
            dropdown.innerHTML = `<option value="">Loading cities...</option>`;
            const token = document.getElementById('postex_token')?.value || 'YzIxZGU5YjY0N2M0NDU1ZGE0ZDM5ZmVlOTA3ZWEwODc6NGI4OTRjNzcyNjRkNDA1NTk3ZDNjODQwNDhhYmQ1Mjg=';
            try {
              const res = await fetch('https://api.postex.pk/services/integration/api/order/v2/get-operational-city', {
                method: 'GET',
                headers: { 'token': token }
              });
              const data = await res.json();
              dropdown.innerHTML = `<option value="">-- Select City --</option>`;
              // API returns: { statusCode: "200", dist: [ { operationalCityName: "Lahore", ... }, ... ] }
              const dist = Array.isArray(data?.dist) ? data.dist : [];
              // Extract names, dedupe, sort
              const names = Array.from(new Set(
                dist
                  .map(c => (c && (c.operationalCityName || c.cityName || c.City || c.city)) || '')
                  .filter(Boolean)
              )).sort((a,b) => a.localeCompare(b));
              names.forEach(name => {
                const opt = document.createElement('option');
                opt.value = name;
                opt.textContent = name;
                dropdown.appendChild(opt);
              });
            } catch (err) {
              console.error('PostEx cities error:', err);
              dropdown.innerHTML = `<option value="">Failed to load cities</option>`;
            }
          }

          (function attachPostexSubmit(){
            const form = document.getElementById('postexForm');
            if (!form) return;
            form.addEventListener('submit', async function(e){
              e.preventDefault();
              const token = document.getElementById('postex_token')?.value || 'YzIxZGU5YjY0N2M0NDU1ZGE0ZDM5ZmVlOTA3ZWEwODc6NGI4OTRjNzcyNjRkNDA1NTk3ZDNjODQwNDhhYmQ1Mjg=';
              const getVal = (id) => (document.getElementById(id)?.value || '').trim();
              // Gather values
              // Normalize city name (some APIs are case/format sensitive)
              const cityNameRaw = getVal('cityName');
              const cityName = cityNameRaw ? cityNameRaw.toUpperCase() : '';
              const customerName = getVal('customerName');
              const customerPhone = getVal('customerPhone');
              const deliveryAddress = getVal('deliveryAddress');
              const orderRefNumber = getVal('orderRefNumber');
              const invoicePayment = Number(getVal('invoicePayment')) || 0;
              const invoiceDivision = Number(getVal('invoiceDivision')) || 1; // default at least 1
              const items = Number(getVal('items')) || 1; // default at least 1
              const orderDetail = getVal('orderDetail');
              const transactionNotes = getVal('transactionNotes');
              const orderType = getVal('orderType') || 'Normal';
              const pickupAddressCode = getVal('pickupAddressCode');
              const storeAddressCode = getVal('storeAddressCode');

              // Validate required fields
              const missing = [];
              if (!orderRefNumber) missing.push('orderRefNumber');
              if (!invoicePayment) missing.push('invoicePayment');
              if (!customerName) missing.push('customerName');
              if (!customerPhone) missing.push('customerPhone');
              if (!deliveryAddress) missing.push('deliveryAddress');
              if (!cityName) missing.push('cityName');
              if (!invoiceDivision) missing.push('invoiceDivision');
              if (!items) missing.push('items');
              if (!orderType) missing.push('orderType');
              // PostEx requires at least one of the codes
              if (!pickupAddressCode && !storeAddressCode) missing.push('pickup/store address code');
              
              // Validate product selection and stock
              const productId = getVal('postex_selected_product_id');
              if (!productId) {
                if (typeof showToast === 'function') showToast('Please select a product from the dropdown', 'error');
                return;
              }
              
              if (missing.length) {
                if (typeof showToast === 'function') showToast(`Please fill required fields: ${missing.join(', ')}`,'error');
                return;
              }

              // Build payload; include optional address codes only if provided
              const payload = {
                cityName,
                customerName,
                customerPhone,
                deliveryAddress,
                invoiceDivision,
                invoicePayment,
                items,
                orderDetail,
                orderRefNumber,
                orderType,
                transactionNotes,
                // Add product info for backend processing
                productId: productId,
                itemsCount: items
              };
              // Prefer storeAddressCode if provided; else use pickupAddressCode
              if (storeAddressCode) payload.storeAddressCode = storeAddressCode;
              else if (pickupAddressCode) payload.pickupAddressCode = pickupAddressCode;
              try {
                const doRequest = async (body) => {
                  const res = await fetch('/api/postex/booking', {
                    method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body)
                  });
                  const data = await res.json();
                  return { res, data };
                };

                // First attempt
                let { res, data } = await doRequest(payload);
                let ok = res.ok && (data?.statusCode === '200' || data?.statusCode === 200);

                // If invalid address code, retry by switching to the other provided code (never send with both null)
                if (!ok && /STORE ADDRESS CODE|PICKUP ADDRESS CODE/i.test(data?.statusMessage || '')) {
                  let retried = false;
                  // If we sent store and user also provided pickup, try pickup only
                  if (payload.storeAddressCode && pickupAddressCode) {
                    const retryPayload = { ...payload };
                    delete retryPayload.storeAddressCode;
                    retryPayload.pickupAddressCode = pickupAddressCode;
                    ({ res, data } = await doRequest(retryPayload));
                    ok = res.ok && (data?.statusCode === '200' || data?.statusCode === 200);
                    retried = true;
                  }
                  // If we sent pickup and user also provided store, try store only
                  if (!ok && payload.pickupAddressCode && storeAddressCode) {
                    const retryPayload2 = { ...payload };
                    delete retryPayload2.pickupAddressCode;
                    retryPayload2.storeAddressCode = storeAddressCode;
                    ({ res, data } = await doRequest(retryPayload2));
                    ok = res.ok && (data?.statusCode === '200' || data?.statusCode === 200);
                    retried = true;
                  }
                  // Do not attempt a retry that removes both codes, as API requires one
                }

                if (ok) {
                  const tn = data?.dist?.trackingNumber || 'N/A';
                  if (typeof showToast === 'function') showToast(`PostEx order created. Tracking: ${tn}`, 'success');
                  window.closePostexModal(); // This will also reset the form
                } else {
                  const msg = data?.statusMessage || data?.message || 'Failed to create order';
                  if (/STORE ADDRESS CODE/i.test(msg)) {
                    // Helpful hint when store code invalid
                    console.warn('Tip: Verify storeAddressCode/pickupAddressCode from PostEx portal (e.g., 001) and ensure it matches your merchant city.');
                  }
                  // Log payload to help diagnose what was sent
                  try { console.debug('PostEx create-order payload sent:', payload); } catch(_) {}
                  if (typeof showToast === 'function') showToast(msg, 'error');
                  console.error('PostEx create-order error:', data);
                }
              } catch (err) {
                console.error('PostEx create order error:', err);
                if (typeof showToast === 'function') showToast('Network error while creating PostEx order', 'error');
              }
            });
          })();

          //_______________________________________________________________ TCS Modal Functions
          function openTcsModal() {
            const modal = document.getElementById('tcsModal');
            if (!modal) return;
            modal.classList.remove('hidden');
            modal.classList.add('flex');
            // Load TCS cities for dropdowns
            setTimeout(() => { try { loadTcsCities(); } catch(_){} }, 100);
          }

          // Cache to avoid re-fetching repeatedly while modal stays open
          let _tcsCityCache = null;
          async function loadTcsCities() {
            const shipperSel = document.getElementById('tcs_shipper_cityname');
            const consigneeSel = document.getElementById('tcs_consignee_cityname');
            const vendorSel = document.getElementById('tcs_vendor_cityname');
            if (!shipperSel || !consigneeSel || !vendorSel) return;

            const setLoading = (el) => { if (el) el.innerHTML = '<option value="">Loading cities...</option>'; };
            const setFailed = (el) => { if (el) el.innerHTML = '<option value="">Failed to load cities</option>'; };
            setLoading(shipperSel); setLoading(consigneeSel); setLoading(vendorSel);

            try {
              let cities = _tcsCityCache;
              if (!cities) {
                const res = await fetch('/api/tcs/cities', {
                  method: 'POST',
                  headers: { 'Content-Type': 'application/json' },
                  body: JSON.stringify({ countrycode: ['PK'] })
                });
                const data = await res.json().catch(()=>({}));
                if (!res.ok) {
                  setFailed(shipperSel); setFailed(consigneeSel); setFailed(vendorSel);
                  if (typeof showToast === 'function') showToast(data?.message || 'Failed to load TCS cities', 'error');
                  return;
                }
                cities = normalizeTcsCities(data);
                _tcsCityCache = cities;
              }

              populateTcsCityDropdown(shipperSel, cities);
              populateTcsCityDropdown(consigneeSel, cities);
              populateTcsCityDropdown(vendorSel, cities, /*required=*/false);

              // Attach code auto-fill on change
              attachCityCodeAutofill('tcs_shipper_cityname', 'tcs_shipper_citycode', cities);
              attachCityCodeAutofill('tcs_consignee_cityname', 'tcs_consignee_citycode', cities);
              attachCityCodeAutofill('tcs_vendor_cityname', 'tcs_vendor_citycode', cities);
            } catch (err) {
              console.error('TCS cities network error:', err);
              setFailed(shipperSel); setFailed(consigneeSel); setFailed(vendorSel);
            }
          }

          function normalizeTcsCities(payload){
            // Try common wrappers
            let arr = Array.isArray(payload) ? payload
              : Array.isArray(payload?.dist) ? payload.dist
              : Array.isArray(payload?.data) ? payload.data
              : Array.isArray(payload?.cities) ? payload.cities
              : [];
            // Map to {name, code}
            const mapped = arr.map(c => {
              const name = c?.cityname || c?.cityName || c?.City || c?.city || '';
              const code = c?.citycode || c?.cityCode || c?.Code || c?.code || '';
              return { name: String(name).trim(), code: String(code||'').trim() };
            }).filter(x => x.name);
            // Dedupe by name, prefer keeping a code if available
            const byName = new Map();
            for (const it of mapped){
              if (!byName.has(it.name) || (!byName.get(it.name).code && it.code)) {
                byName.set(it.name, it);
              }
            }
            return Array.from(byName.values()).sort((a,b)=>a.name.localeCompare(b.name));
          }

          function populateTcsCityDropdown(selectEl, cities, required=true){
            if (!selectEl) return;
            const placeholder = required ? 'Select City' : 'Select City (optional)';
            selectEl.innerHTML = `<option value="">${placeholder}</option>`;
            cities.forEach(({name, code}) => {
              const opt = document.createElement('option');
              opt.value = name;
              opt.textContent = code ? `${name} (${code})` : name;
              opt.dataset.code = code || '';
              selectEl.appendChild(opt);
            });
          }

          function attachCityCodeAutofill(selectId, codeInputId, cities){
            const sel = document.getElementById(selectId);
            const codeEl = document.getElementById(codeInputId);
            if (!sel || !codeEl) return;
            const byName = new Map(cities.map(c=>[c.name, c.code]));
            sel.addEventListener('change', function(){
              const name = sel.value;
              const code = byName.get(name) || '';
              if (code) codeEl.value = code;
            });
          }
          function closeTcsModal() {
            const modal = document.getElementById('tcsModal');
            if (!modal) return;
            modal.classList.remove('flex');
            modal.classList.add('hidden');
            
            // Reset form and clear product selection
            const form = document.getElementById('tcsForm');
            if (form) form.reset();
            
            // Clear product selection
            const productIdInput = document.getElementById('tcs_selected_product_id');
            const stockInfo = document.getElementById('tcs_stock_info');
            const dropdown = document.getElementById('tcs_product_dropdown');
            
            if (productIdInput) productIdInput.value = '';
            if (stockInfo) stockInfo.classList.add('hidden');
            if (dropdown) dropdown.classList.add('hidden');
          }

          // Product search functionality for TCS Content Description
          (function initTcsProductSearch() {
            const searchInput = document.getElementById('tcs_ship_contentdesc');
            const dropdown = document.getElementById('tcs_product_dropdown');
            const productIdInput = document.getElementById('tcs_selected_product_id');
            const stockInfo = document.getElementById('tcs_stock_info');
            const stockText = document.getElementById('tcs_stock_text');
            const piecesInput = document.getElementById('tcs_ship_pieces');
            
            if (!searchInput || !dropdown || !productIdInput) return;
            
            let searchTimeout;
            let selectedProduct = null;
            
            // Search products as user types
            searchInput.addEventListener('input', function() {
              const query = this.value.trim();
              
              clearTimeout(searchTimeout);
              
              if (query.length < 1) {
                dropdown.classList.add('hidden');
                productIdInput.value = '';
                selectedProduct = null;
                stockInfo.classList.add('hidden');
                return;
              }
              
              searchTimeout = setTimeout(async () => {
                try {
                  const response = await fetch(`/api/products/search?q=${encodeURIComponent(query)}`);
                  const products = await response.json();
                  
                  dropdown.innerHTML = '';
                  
                  if (products.length === 0) {
                    dropdown.innerHTML = '<div class="p-3 text-gray-500">No products found</div>';
                  } else {
                    products.forEach(product => {
                      const div = document.createElement('div');
                      div.className = 'p-3 hover:bg-gray-100 cursor-pointer border-b border-gray-100 last:border-b-0';
                      div.innerHTML = `
                        <div class="font-medium">${product.name}</div>
                        <div class="text-sm text-gray-600">Stock: ${product.stock} available</div>
                      `;
                      div.addEventListener('click', () => selectProduct(product));
                      dropdown.appendChild(div);
                    });
                  }
                  
                  dropdown.classList.remove('hidden');
                } catch (error) {
                  console.error('Product search error:', error);
                  dropdown.innerHTML = '<div class="p-3 text-red-500">Error searching products</div>';
                  dropdown.classList.remove('hidden');
                }
              }, 300);
            });
            
            // Select a product
            function selectProduct(product) {
              selectedProduct = product;
              searchInput.value = product.name;
              productIdInput.value = product._id;
              dropdown.classList.add('hidden');
              
              // Show stock information
              stockText.textContent = `Available stock: ${product.stock}`;
              stockInfo.classList.remove('hidden');
              
              // Validate pieces against stock
              validateStock();
            }
            
            // Validate stock when pieces change
            if (piecesInput) {
              piecesInput.addEventListener('input', validateStock);
            }
            
            function validateStock() {
              if (!selectedProduct || !piecesInput) return;
              
              const pieces = parseInt(piecesInput.value) || 0;
              const available = selectedProduct.stock;
              
              if (pieces > available) {
                stockText.textContent = `Insufficient stock! Available: ${available}, Requested: ${pieces}`;
                stockText.className = 'text-red-600';
                piecesInput.classList.add('border-red-500');
              } else {
                stockText.textContent = `Available stock: ${available}`;
                stockText.className = 'text-green-600';
                piecesInput.classList.remove('border-red-500');
              }
            }
            
            // Hide dropdown when clicking outside
            document.addEventListener('click', function(e) {
              if (!searchInput.contains(e.target) && !dropdown.contains(e.target)) {
                dropdown.classList.add('hidden');
              }
            });
          })();

          // Handle TCS form submission
          (function attachTcsSubmit(){
            const form = document.getElementById('tcsForm');
            if (!form) return;
            form.addEventListener('submit', async function(e){
              e.preventDefault();
              const get = (id) => (document.getElementById(id)?.value || '').trim();
              const consignmentno = get('tcs_consignmentno');

              // Shipper
              const shipperinfo = {
                tcsaccount: get('tcs_shipper_tcsaccount'),
                shippername: get('tcs_shipper_name'),
                address1: get('tcs_shipper_address1'),
                address2: get('tcs_shipper_address2') || undefined,
                address3: get('tcs_shipper_address3') || undefined,
                zip: get('tcs_shipper_zip') || undefined,
                countrycode: get('tcs_shipper_countrycode') || 'PK',
                countryname: get('tcs_shipper_countryname') || 'Pakistan',
                citycode: get('tcs_shipper_citycode') || undefined,
                cityname: get('tcs_shipper_cityname'),
                mobile: get('tcs_shipper_mobile')
              };

              // Consignee
              const consigneeinfo = {
                consigneecode: get('tcs_consignee_code') || undefined,
                firstname: get('tcs_consignee_firstname'),
                middlename: get('tcs_consignee_middlename'),
                lastname: get('tcs_consignee_lastname') || undefined,
                address1: get('tcs_consignee_address1'),
                address2: get('tcs_consignee_address2') || undefined,
                address3: get('tcs_consignee_address3') || undefined,
                zip: get('tcs_consignee_zip') || undefined,
                countrycode: get('tcs_consignee_countrycode') || 'PK',
                countryname: get('tcs_consignee_countryname') || 'Pakistan',
                citycode: get('tcs_consignee_citycode') || undefined,
                cityname: get('tcs_consignee_cityname'),
                email: get('tcs_consignee_email') || undefined,
                areacode: get('tcs_consignee_areacode') || undefined,
                areaname: get('tcs_consignee_areaname') || undefined,
                blockcode: get('tcs_consignee_blockcode') || undefined,
                blockname: get('tcs_consignee_blockname') || undefined,
                lat: get('tcs_consignee_lat') || undefined,
                lng: get('tcs_consignee_lng') || undefined,
                landmark: get('tcs_consignee_landmark') || undefined,
                mobile: get('tcs_consignee_mobile')
              };

              // Vendor (optional)
              const vendorinfo = {
                name: get('tcs_vendor_name') || undefined,
                address1: get('tcs_vendor_address1') || undefined,
                address2: get('tcs_vendor_address2') || undefined,
                address3: get('tcs_vendor_address3') || undefined,
                citycode: get('tcs_vendor_citycode') || undefined,
                cityname: get('tcs_vendor_cityname') || undefined,
                mobile: get('tcs_vendor_mobile') || undefined
              };

              // Shipment
              const shipmentinfo = {
                costcentercode: get('tcs_ship_costcentercode'),
                servicecode: get('tcs_ship_servicecode'),
                referenceno: get('tcs_ship_referenceno') || undefined,
                contentdesc: get('tcs_ship_contentdesc') || undefined,
                currency: get('tcs_ship_currency') || 'PKR',
                codamount: Number(get('tcs_ship_codamount') || '0'),
                weightinkg: Number(get('tcs_ship_weightinkg') || '0.5'),
                pieces: Number(get('tcs_ship_pieces') || '1'),
                fragile: (get('tcs_ship_fragile') || 'false') === 'true',
                remarks: get('tcs_ship_remarks') || undefined
              };

              // SKU (optional single)
              const skuDesc = get('tcs_sku_description');
              const skuQty = get('tcs_sku_quantity');
              const skuWeight = get('tcs_sku_weight');
              const skuUom = get('tcs_sku_uom');
              const skuUnitPrice = get('tcs_sku_unitprice');
              const skus = [];
              if (skuDesc || skuQty || skuWeight || skuUom || skuUnitPrice) {
                skus.push({
                  description: skuDesc || undefined,
                  quantity: skuQty ? Number(skuQty) : undefined,
                  weight: skuWeight ? Number(skuWeight) : undefined,
                  uom: skuUom || undefined,
                  unitprice: skuUnitPrice ? Number(skuUnitPrice) : undefined
                });
              }
              if (skus.length) shipmentinfo.skus = skus;

              // Validate requireds
              const missing = [];
              if (!shipperinfo.tcsaccount) missing.push('tcsaccount');
              if (!shipperinfo.shippername) missing.push('shippername');
              if (!shipperinfo.address1) missing.push('shipper.address1');
              if (!shipperinfo.countrycode) missing.push('shipper.countrycode');
              if (!shipperinfo.countryname) missing.push('shipper.countryname');
              if (!shipperinfo.cityname) missing.push('shipper.cityname');
              if (!shipperinfo.mobile) missing.push('shipper.mobile');
              if (!consigneeinfo.firstname) missing.push('consignee.firstname');
              if (!consigneeinfo.middlename) missing.push('consignee.middlename');
              if (!consigneeinfo.address1) missing.push('consignee.address1');
              if (!consigneeinfo.countrycode) missing.push('consignee.countrycode');
              if (!consigneeinfo.countryname) missing.push('consignee.countryname');
              if (!consigneeinfo.cityname) missing.push('consignee.cityname');
              if (!consigneeinfo.mobile) missing.push('consignee.mobile');
              if (!shipmentinfo.costcentercode) missing.push('costcentercode');
              if (!shipmentinfo.servicecode) missing.push('servicecode');
              if (shipmentinfo.servicecode && (shipmentinfo.servicecode.length < 1 || shipmentinfo.servicecode.length > 6)) missing.push('servicecode (1-6 chars)');
              if (!shipmentinfo.currency) missing.push('currency');
              if (!(shipmentinfo.codamount >= 0)) missing.push('codamount');
              if (!(shipmentinfo.weightinkg >= 0.5)) missing.push('weightinkg>=0.5');
              if (!(shipmentinfo.pieces >= 1)) missing.push('pieces>=1');
              
              // Validate product selection and stock
              const productId = get('tcs_selected_product_id');
              if (!productId) {
                if (typeof showToast === 'function') showToast('Please select a product from the dropdown', 'error');
                return;
              }
              
              if (missing.length) {
                if (typeof showToast === 'function') showToast(`Please fill: ${missing.join(', ')}`, 'error');
                return;
              }

              const body = { consignmentno, shipperinfo, consigneeinfo, vendorinfo, shipmentinfo, productId, pieces: shipmentinfo.pieces };

              try {
                const res = await fetch('/api/tcs/booking', {
                  method: 'POST',
                  headers: { 'Content-Type': 'application/json' },
                  body: JSON.stringify(body)
                });
                const data = await res.json().catch(()=>({}));
                if (res.ok) {
                  if (typeof showToast === 'function') showToast('TCS order created successfully', 'success');
                  try { console.log('TCS create-order success:', data); } catch(_) {}
                  closeTcsModal(); // This will also reset the form
                } else {
                  const msg = data?.message || data?.statusMessage || data?.StatusMessage || 'Failed to create TCS order';
                  if (typeof showToast === 'function') showToast(msg, 'error');
                  console.error('TCS create-order error:', data);
                  try {
                    const safeBody = { ...body };
                    console.debug('TCS payload sent:', safeBody);
                  } catch(_){ /* noop */ }
                }
              } catch (err) {
                console.error('TCS create-order network error:', err);
                if (typeof showToast === 'function') showToast('Network error while creating TCS order', 'error');
              }
            });
          })();

          // PostEx Results: fetch and render orders
          (function initPostexResults(){
            const form = document.getElementById('postexResultsForm');
            const tbody = document.getElementById('postexResultsBody');
            if (!form || !tbody) return;

            const statusEl = document.getElementById('postexOrderStatusID');
            const fromEl = document.getElementById('postexFromDate');
            const toEl = document.getElementById('postexToDate');

            const fmt = (d)=>{
              const y=d.getFullYear();
              const m=String(d.getMonth()+1).padStart(2,'0');
              const day=String(d.getDate()).padStart(2,'0');
              return `${y}-${m}-${day}`;
            };

            // set defaults: last 7 days
            try{
              const today=new Date();
              const weekAgo=new Date();
              weekAgo.setDate(today.getDate()-7);
              if(fromEl && !fromEl.value) fromEl.value=fmt(weekAgo);
              if(toEl && !toEl.value) toEl.value=fmt(today);
            }catch(_){/* noop */}

            async function loadOrders(){
              const token = document.getElementById('postex_token')?.value || 'YzIxZGU5YjY0N2M0NDU1ZGE0ZDM5ZmVlOTA3ZWEwODc6NGI4OTRjNzcyNjRkNDA1NTk3ZDNjODQwNDhhYmQ1Mjg=';
              const orderStatusID = Number(statusEl?.value||0) || 0;
              const fromDate = fromEl?.value || '';
              const toDate = toEl?.value || '';

              // Validate required params
              if(!fromDate || !toDate){
                if (typeof showToast === 'function') showToast('Please select From and To dates','error');
                return;
              }

              // Some environments require different param names; send all for compatibility
              const url = `https://api.postex.pk/services/integration/api/order/v1/get-all-order?orderStatusID=${encodeURIComponent(orderStatusID)}&orderStatusId=${encodeURIComponent(orderStatusID)}&fromDate=${encodeURIComponent(fromDate)}&toDate=${encodeURIComponent(toDate)}&startDate=${encodeURIComponent(fromDate)}&endDate=${encodeURIComponent(toDate)}`;
              try{
                tbody.innerHTML = `<tr><td class="px-3 py-2" colspan="11">Loading...</td></tr>`;
                const res = await fetch(url, { method:'GET', headers:{ token } });
                const data = await res.json();
                if(!(res.ok) || !(data?.statusCode==="200" || data?.statusCode===200)){
                  const msg = data?.statusMessage || data?.message || 'Failed to load orders';
                  tbody.innerHTML = `<tr><td class="px-3 py-2 text-red-600" colspan="11">${msg}</td></tr>`;
                  return;
                }
                const dist = Array.isArray(data?.dist)? data.dist : [];
                if(dist.length===0){
                  tbody.innerHTML = `<tr><td class="px-3 py-2" colspan="11">No data</td></tr>`;
                  return;
                }
                const rowsHtml = dist.map(item=>{
                  const trk = item?.trackingNumber || item?.trackingResponse?.trackingNumber || '';
                  const r = item?.trackingResponse || {};
                  const amount = (r.invoicePayment!=null)? r.invoicePayment : '';
                  const date = r.transactionDate || '';
                  const cust = r.customerName || '';
                  const phone = r.customerPhone || '';
                  const city = r.cityName || '';
                  const addr = r.deliveryAddress || '';
                  const status = r.transactionStatus || '';
                  const ref = r.orderRefNumber || '';
                  const items = r.items!=null ? r.items : '';
                  const div = r.invoiceDivision!=null ? r.invoiceDivision : '';
                  return `<tr class="border-b">
                    <td class=\"px-3 py-2 whitespace-nowrap\">${trk}</td>
                    <td class=\"px-3 py-2\">${cust}</td>
                    <td class=\"px-3 py-2\">${phone}</td>
                    <td class=\"px-3 py-2\">${city}</td>
                    <td class=\"px-3 py-2\">${addr}</td>
                    <td class=\"px-3 py-2\">${amount}</td>
                    <td class=\"px-3 py-2\">${status}</td>
                    <td class=\"px-3 py-2\">${ref}</td>
                    <td class=\"px-3 py-2\">${items}</td>
                    <td class=\"px-3 py-2\">${div}</td>
                    <td class=\"px-3 py-2\">${date}</td>
                  </tr>`;
                }).join('');
                tbody.innerHTML = rowsHtml;
              }catch(err){
                console.error('PostEx get-all-order error:', err);
                tbody.innerHTML = `<tr><td class=\"px-3 py-2 text-red-600\" colspan=\"11\">Network error while loading orders</td></tr>`;
              }
            }

            form.addEventListener('submit', function(e){ e.preventDefault(); loadOrders(); });
            // initial load with defaults
            loadOrders();
          })();

          // Handle Leopards form submission
          document.getElementById('leopardsForm').addEventListener('submit', async function (e) {
            e.preventDefault();

            const formData = {
              api_key: document.getElementById('api_key').value,
              api_password: document.getElementById('api_password').value,
              booked_packet_weight: parseInt(document.getElementById('booked_packet_weight').value),
              booked_packet_vol_weight_w: document.getElementById('booked_packet_vol_weight_w').value || '',
              booked_packet_vol_weight_h: document.getElementById('booked_packet_vol_weight_h').value || '',
              booked_packet_vol_weight_l: document.getElementById('booked_packet_vol_weight_l').value || '',
              booked_packet_no_piece: parseInt(document.getElementById('booked_packet_no_piece').value),
              booked_packet_collect_amount: document.getElementById('booked_packet_collect_amount').value || 0,
              booked_packet_order_id: document.getElementById('booked_packet_order_id').value || '',
              origin_city: document.getElementById('origin_city').value,
              destination_city: document.getElementById('destination_city').value,
              shipment_id: parseInt(document.getElementById('shipment_id').value),
              shipment_name_eng: document.getElementById('shipment_name_eng').value,
              shipment_email: document.getElementById('shipment_email').value,
              shipment_phone: document.getElementById('shipment_phone').value,
              shipment_address: document.getElementById('shipment_address').value,
              consignment_name_eng: document.getElementById('consignment_name_eng').value,
              consignment_email: document.getElementById('consignment_email').value || '',
              consignment_phone: document.getElementById('consignment_phone').value,
              consignment_phone_two: document.getElementById('consignment_phone_two').value || '',
              consignment_phone_three: document.getElementById('consignment_phone_three').value || '',
              consignment_address: document.getElementById('consignment_address').value,
              special_instructions: document.getElementById('special_instructions').value || '',
              shipment_type: document.getElementById('shipment_type').value,
              return_address: document.getElementById('return_address').value || '',
              return_city: document.getElementById('return_city').value || '',
              is_vpc: parseInt(document.getElementById('is_vpc').value)
            };

            try {
              const response = await fetch('https://merchantapistaging.leopardscourier.com/api/bookPacket/format/json/', {
                method: 'POST',
                headers: {
                  'Content-Type': 'application/json',
                },
                body: JSON.stringify(formData)
              });

              const result = await response.json();

              if (result.status === 1) {
                alert('Package booked successfully! Tracking ID: ' + (result.tracking_id || 'N/A'));
                closeLeopardsModal();
                // Reset form
                document.getElementById('leopardsForm').reset();
              } else {
                alert('Error booking package: ' + (result.error || 'Unknown error'));
              }
            } catch (error) {
              console.error('Error submitting form:', error);
              alert('Error submitting form. Please try again.');
            }
          });

          // Tab Management Functions
          function showTab(tabName) {
            // Hide all tab contents
            const tabContents = document.querySelectorAll('.tab-content');
            tabContents.forEach(content => content.classList.add('hidden'));

            // Remove active state from all tab buttons
            const tabButtons = document.querySelectorAll('.tab-button');
            tabButtons.forEach(button => {
              button.classList.remove('border-green-500', 'text-green-600');
              button.classList.add('border-transparent', 'text-gray-500');
            });

            // Show selected tab content
            const selectedContent = document.getElementById(tabName + '-content');
            if (selectedContent) {
              selectedContent.classList.remove('hidden');
            }

            // Activate selected tab button
            const selectedButton = document.getElementById(tabName + '-tab');
            if (selectedButton) {
              selectedButton.classList.remove('border-transparent', 'text-gray-500');
              selectedButton.classList.add('border-green-500', 'text-green-600');
            }

            // Auto-load data for specific tabs
            if (tabName === 'leopards') {
              // Auto-load Leopards data when tab is opened
              setTimeout(() => {
                fetchLeopardsData();
              }, 100);
            }
            if (tabName === 'mp') {
              // Auto-load M&P QSR report when tab is opened
              setTimeout(() => {
                fetchMnpReport();
              }, 100);
            }
          }

          // Leopards API Integration
          let leopardsData = [];
          let filteredData = [];
          let currentPage = 1;
          const pageSize = 50; // max 50 per page

          async function fetchLeopardsData() {
            const resultDiv = document.getElementById('leopards-result');
            resultDiv.innerHTML = '<div class="text-center py-8"><i class="fas fa-spinner fa-spin text-4xl text-green-500 mb-3"></i><p>Loading shipment data...</p></div>';

            try {
              const params = new URLSearchParams({
                api_key: 'c7ab1d5f6b35f23b496c9cc24d4715c1',
                api_password: '123456',
                from_date: document.getElementById('from-date').value,
                to_date: document.getElementById('to-date').value
              });
// https://merchantapistaging.leopardscourier.com/api/getBookedPacketLastStatus/format/json/
              const listResponse = await fetch(`https://merchantapistaging.leopardscourier.com/api/getBookedPacketLastStatus/format/json/?${params.toString()}`, {
                method: "GET",
                headers: { "Content-Type": "application/json" }
              });

              const listData = await listResponse.json();

              if (!listData || !listData.packet_list || listData.packet_list.length === 0) {
                resultDiv.innerHTML = '<div class="text-center text-gray-500 py-8"><i class="fas fa-exclamation-triangle text-4xl mb-3"></i><p>No booked packets found.</p></div>';
                return;
              }

              // Extract unique order IDs
              const orderIds = [...new Set(
                listData.packet_list
                  .map(packet => packet.booked_packet_order_id)
                  .filter(id => id && id.trim() !== "")
              )];

              if (orderIds.length === 0) {
                resultDiv.innerHTML = '<div class="text-center text-gray-500 py-8"><i class="fas fa-exclamation-triangle text-4xl mb-3"></i><p>No valid order IDs found in booked packets.</p></div>';
                return;
              }

              // Helper: split array into chunks of max size
              function chunkArray(arr, size) {
                const chunks = [];
                for (let i = 0; i < arr.length; i += size) {
                  chunks.push(arr.slice(i, i + size));
                }
                return chunks;
              }

              const chunks = chunkArray(orderIds, 100); // max 100 per API call
              let allData = [];

              for (const chunk of chunks) {
                const detailPayload = {
                  api_key: 'c7ab1d5f6b35f23b496c9cc24d4715c1',
                  api_password: '123456',
                  shipment_order_id: chunk
                };

                const detailResponse = await fetch("https://merchantapistaging.leopardscourier.com/api/getShipmentDetailsByOrderID/format/json/", {
                  method: "POST",
                  headers: { "Content-Type": "application/json" },
                  body: JSON.stringify(detailPayload)
                });

                const detailData = await detailResponse.json();

                if (!detailData || detailData.status !== 1) {
                  resultDiv.innerHTML = `<div class="text-center text-red-500 py-8"><i class="fas fa-exclamation-triangle text-4xl mb-3"></i><p>Error fetching shipment details: ${detailData.error || "Unknown error"}</p></div>`;
                  return;
                }

                if (detailData.data && detailData.data.length > 0) {
                  allData = allData.concat(detailData.data);
                }
              }

              if (allData.length === 0) {
                resultDiv.innerHTML = '<div class="text-center text-gray-500 py-8"><i class="fas fa-exclamation-triangle text-4xl mb-3"></i><p>No shipment details found for given order IDs.</p></div>';
                return;
              }

              leopardsData = allData;
              filteredData = [...allData];
              displayLeopardsData(filteredData);

            } catch (err) {
              resultDiv.innerHTML = `<div class="text-center text-red-500 py-8"><i class="fas fa-exclamation-triangle text-4xl mb-3"></i><p>Error: ${err.message}</p></div>`;
            }
          }

          function displayLeopardsData(data) {
            const resultDiv = document.getElementById('leopards-result');

            if (data.length === 0) {
              resultDiv.innerHTML = '<div class="text-center text-gray-500 py-8"><i class="fas fa-search text-4xl mb-3"></i><p>No shipments match the current filters.</p></div>';
              return;
            }

            // Fixed columns and labels (as requested)
            const columns = [
              { key: 'booking_date', label: 'BOOKING DATE' },
              { key: 'track_number', label: 'TRACK NUMBER' },
              { key: 'track_number_short', label: 'TRACK NUMBER SHORT' },
              { key: 'booked_packet_weight', label: 'BOOKED PACKET WEIGHT' },
              { key: 'booked_packet_no_piece', label: 'BOOKED PACKET NO PIECE' },
              { key: 'booked_packet_collect_amount', label: 'BOOKED PACKET COLLECT AMOUNT' },
              { key: 'booked_packet_order_id', label: 'BOOKED PACKET ORDER ID' },
              { key: 'origin_city', label: 'ORIGIN CITY' },
              { key: 'destination_city', label: 'DESTINATION CITY' },
              { key: 'shipment_name_eng', label: 'SHIPMENT NAME ENG' },
              { key: 'shipment_email', label: 'SHIPMENT EMAIL' },
              { key: 'shipment_phone', label: 'SHIPMENT PHONE' },
              { key: 'shipment_address', label: 'SHIPMENT ADDRESS' },
              { key: 'consignment_name_eng', label: 'CONSIGNMENT NAME ENG' },
              { key: 'consignment_email', label: 'CONSIGNMENT EMAIL' },
              { key: 'consignment_phone', label: 'CONSIGNMENT PHONE' },
              { key: 'consignment_phone_two', label: 'CONSIGNMENT PHONE TWO' },
              { key: 'consignment_phone_three', label: 'CONSIGNMENT PHONE THREE' },
              { key: 'consignment_address', label: 'CONSIGNMENT ADDRESS' },
              { key: 'special_instructions', label: 'SPECIAL INSTRUCTIONS' },
              { key: 'shipment_type_name', label: 'SHIPMENT TYPE NAME' },
              { key: 'booked_packet_status', label: 'BOOKED PACKET STATUS' },
              { key: 'delivery_date', label: 'DELIVERY DATE' },
              { key: 'return_date', label: 'RETURN DATE' },
              { key: 'invoice_number', label: 'INVOICE NUMBER' },
              { key: 'invoice_date', label: 'INVOICE DATE' }
            ];

            // Pagination calculations
            const total = data.length;
            const totalPages = Math.max(1, Math.ceil(total / pageSize));
            if (currentPage > totalPages) currentPage = totalPages;
            if (currentPage < 1) currentPage = 1;
            const startIdx = (currentPage - 1) * pageSize;
            const endIdx = Math.min(startIdx + pageSize, total);
            const pageItems = data.slice(startIdx, endIdx);

            // Build HTML table
            let html = "<div class='overflow-x-auto'><table class='min-w-full bg-white border border-gray-200 rounded-lg'>";
            html += "<thead><tr class='bg-gradient-to-r from-green-600 to-green-700 text-white'>";
            columns.forEach(col => {
              html += `<th class=\"px-4 py-3 text-left text-xs md:text-sm font-semibold border-b border-green-600 whitespace-nowrap\">${col.label}</th>`;
            });
            html += "</tr></thead><tbody>";

            pageItems.forEach((item, index) => {
              const rowClass = index % 2 === 0 ? 'bg-gray-50' : 'bg-white';
              html += `<tr class="${rowClass} hover:bg-gray-100 transition-colors">`;
              columns.forEach(col => {
                // Prefer exact key; fallback to potential alternative casings
                let val = item[col.key];
                if (val === undefined) {
                  const alt1 = col.key.replace(/_([a-z])/g, (_, c) => c.toUpperCase()); // snake_case -> camelCase
                  const alt2 = col.key.toUpperCase();
                  val = item[alt1] ?? item[alt2];
                }
                if (val === null || val === undefined) val = '';
                html += `<td class=\"px-4 py-3 text-xs md:text-sm text-gray-700 border-b border-gray-200 whitespace-nowrap\">${val}</td>`;
              });
              html += "</tr>";
            });

            html += "</tbody></table></div>";
            html += `<div class=\"mt-4 text-sm text-gray-600\">Showing ${startIdx + 1}–${endIdx} of ${total} shipments (Page ${currentPage} of ${totalPages})</div>`;

            // Pagination controls
            html += `<div class=\"mt-3 flex items-center gap-2\">`;
            const disabledPrev = currentPage === 1 ? 'opacity-50 cursor-not-allowed' : '';
            const disabledNext = currentPage === totalPages ? 'opacity-50 cursor-not-allowed' : '';
            html += `<button class=\"px-3 py-1 rounded border text-sm ${disabledPrev}\" onclick=\"goToPage(${currentPage - 1})\">Prev</button>`;
            const windowSize = 5;
            let startPage = Math.max(1, currentPage - Math.floor(windowSize / 2));
            let endPage = Math.min(totalPages, startPage + windowSize - 1);
            startPage = Math.max(1, Math.min(startPage, endPage - windowSize + 1));
            for (let p = startPage; p <= endPage; p++) {
              const active = p === currentPage ? 'bg-green-600 text-white' : 'bg-white text-gray-700 hover:bg-gray-100';
              html += `<button class=\"px-3 py-1 rounded border text-sm ${active}\" onclick=\"goToPage(${p})\">${p}</button>`;
            }
            html += `<button class=\"px-3 py-1 rounded border text-sm ${disabledNext}\" onclick=\"goToPage(${currentPage + 1})\">Next</button>`;
            html += `</div>`;

            resultDiv.innerHTML = html;
          }

          function goToPage(page) {
            const totalPages = Math.max(1, Math.ceil(filteredData.length / pageSize));
            if (typeof page !== 'number') return;
            if (page < 1 || page > totalPages) return;
            currentPage = page;
            displayLeopardsData(filteredData);
          }

          function applyFilters() {
            const fromDate = document.getElementById('from-date').value;
            const toDate = document.getElementById('to-date').value;
            const searchTerm = document.getElementById('search-filter').value.toLowerCase();

            // Helper: robust date parser supporting YYYY-MM-DD, MM/DD/YYYY, YYYY/MM/DD
            function parseDateSafe(val) {
              if (!val) return null;
              if (val instanceof Date) return isNaN(val) ? null : val;
              if (typeof val !== 'string') {
                const d = new Date(val);
                return isNaN(d) ? null : d;
              }
              const s = val.trim();
              // Try native Date first
              let d = new Date(s);
              if (!isNaN(d)) return d;
              // Try split patterns
              // YYYY-MM-DD or YYYY/MM/DD
              let m = s.match(/^\d{4}[-\/]\d{1,2}[-\/]\d{1,2}$/);
              if (m) {
                const parts = s.split(/[-\/]/).map(Number);
                d = new Date(parts[0], parts[1] - 1, parts[2]);
                return isNaN(d) ? null : d;
              }
              // MM/DD/YYYY
              m = s.match(/^\d{1,2}[-\/]\d{1,2}[-\/]\d{4}$/);
              if (m) {
                const parts = s.split(/[-\/]/).map(Number);
                d = new Date(parts[2], parts[0] - 1, parts[1]);
                return isNaN(d) ? null : d;
              }
              return null;
            }

            // Normalize filter dates
            const from = fromDate ? parseDateSafe(fromDate) : null;
            const to = toDate ? parseDateSafe(toDate) : null;
            if (to) {
              // Set to end of day for inclusive filtering
              to.setHours(23, 59, 59, 999);
            }

            // Keys to try for date fields in each item
            const dateKeys = [
              'booking_date', 'BOOKING_DATE', 'BookingDate',
              'delivery_date', 'DELIVERY_DATE', 'DeliveryDate',
              'return_date', 'RETURN_DATE', 'ReturnDate'
            ];

            filteredData = leopardsData.filter(item => {
              // Search filter across all values
              const matchesSearch = searchTerm
                ? Object.values(item).some(value => value && value.toString().toLowerCase().includes(searchTerm))
                : true;

              // Date filter: use first available date field
              let itemDate = null;
              for (const k of dateKeys) {
                if (item[k] !== undefined && item[k] !== null && item[k] !== '') {
                  itemDate = parseDateSafe(item[k]);
                  if (itemDate) break;
                }
              }

              let matchesDate = true;
              if (from || to) {
                // If no date in item, treat as non-matching when filters are set
                if (!itemDate) {
                  matchesDate = false;
                } else {
                  if (from && itemDate < from) matchesDate = false;
                  if (to && itemDate > to) matchesDate = false;
                }
              }

              return matchesSearch && matchesDate;
            });

            currentPage = 1;
            displayLeopardsData(filteredData);
          }

          function clearFilters() {
            document.getElementById('from-date').value = '2019-12-31';
            document.getElementById('to-date').value = '2025-12-31';
            document.getElementById('search-filter').value = '';

            filteredData = [...leopardsData];
            currentPage = 1;
            displayLeopardsData(filteredData);
          }

          // M&P QSR Report Integration
          async function fetchMnpReport() {
            const resultDiv = document.getElementById('mnp-result');
            if (!resultDiv) return;
            // credentials work behind the scenes (hidden from UI)
            const username = 'ALI.RAZA_18O59';
            const password = 'AliRaza@123';
            const locationID = (document.getElementById('mnp-location')?.value || '').trim();
            const year = parseInt(document.getElementById('mnp-year')?.value || '0', 10);
            const month = parseInt(document.getElementById('mnp-month')?.value || '0', 10);

            const errors = [];
            if (!locationID) errors.push('locationID is required');
            if (!(year >= 2017 && year <= 2025)) errors.push('Year must be between 2017 and 2025');
            if (!(month >= 1 && month <= 12)) errors.push('MonthNumber must be 1-12');

            if (errors.length) {
              if (typeof showToast === 'function') showToast(errors.join('\n'), 'error');
              resultDiv.innerHTML = `<div class="text-center text-red-500 py-6"><i class='fas fa-exclamation-triangle text-3xl mb-2'></i><p>${errors.join('<br>')}</p></div>`;
              return;
            }

            resultDiv.innerHTML = '<div class="text-center py-8"><i class="fas fa-spinner fa-spin text-4xl text-blue-600 mb-3"></i><p>Loading M&P QSR report...</p></div>';

            const payload = {
              UserName: username,
              Password: password,
              MonthNumber: month,
              Year: year,
              locationID: locationID
            };

            try {
              const resp = await fetch('https://mnpcourier.com/mycodapi/api/Reports/QSR_Report', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
              });

              const data = await resp.json();

              if (!resp.ok) {
                throw new Error(data?.message || `HTTP ${resp.status}`);
              }

              if (!data || data.isSuccess === false) {
                const msg = data?.message || 'Report request failed';
                if (typeof showToast === 'function') showToast(msg, 'error');
                resultDiv.innerHTML = `<div class="text-center text-red-500 py-8"><i class='fas fa-exclamation-circle text-4xl mb-2'></i><p>${msg}</p></div>`;
                return;
              }

              displayMnpReport(data);
              if (typeof showToast === 'function') showToast('M&P report loaded', 'success');
            } catch (err) {
              console.error('QSR_Report error:', err);
              if (typeof showToast === 'function') showToast(err.message || 'Failed to load report', 'error');
              resultDiv.innerHTML = `<div class="text-center text-red-500 py-8"><i class='fas fa-exclamation-triangle text-4xl mb-2'></i><p>${err.message || 'Failed to load report'}</p></div>`;
            }
          }

          function displayMnpReport(report) {
            const resultDiv = document.getElementById('mnp-result');
            if (!resultDiv) return;

            const details = Array.isArray(report?.Details) ? report.Details : [];
            if (details.length === 0) {
              resultDiv.innerHTML = '<div class="text-center text-gray-500 py-8"><i class="fas fa-search text-4xl mb-3"></i><p>No data found for given filters.</p></div>';
              return;
            }

            const columns = [
              { key: 'consignmentNumber', label: 'Consignment Number' },
              { key: 'BookingDate', label: 'Booking Date' },
              { key: 'OrgZone', label: 'Origin Zone' },
              { key: 'OrgBranch', label: 'Origin Branch' },
              { key: 'consignee', label: 'Consignee' },
              { key: 'orderRefNo', label: 'Order Ref No' },
              { key: 'SERVICE', label: 'Service' },
              { key: 'DESTZONE', label: 'Dest Zone' },
              { key: 'DESTINATION', label: 'Destination' },
              { key: 'WEIGHT', label: 'Weight' },
              { key: 'receivedBy', label: 'Received By' },
              { key: 'TIME', label: 'Time' },
              { key: 'deliveryDate', label: 'Delivery Date' },
              { key: 'CODAmount', label: 'COD Amount' },
              { key: 'PaymentMode', label: 'Payment Mode' },
              { key: 'RRStatus', label: 'RR Status' },
              { key: 'CHEQUENO', label: 'Cheque No' }
            ];

            let html = '';
            html += '<div class="mb-4 text-sm text-gray-700">';
            if (report.AccountNumber) html += `<div><span class='font-semibold'>Account Number:</span> ${report.AccountNumber}</div>`;
            if (report.ClientName) html += `<div><span class='font-semibold'>Client Name:</span> ${report.ClientName}</div>`;
            if (report.message) html += `<div><span class='font-semibold'>Message:</span> ${report.message}</div>`;
            html += '</div>';

            html += "<div class='overflow-x-auto'><table class='min-w-full bg-white border border-gray-200 rounded-lg'>";
            html += "<thead><tr class='bg-gradient-to-r from-blue-600 to-blue-700 text-white'>";
            columns.forEach(col => {
              html += `<th class=\"px-4 py-3 text-left text-xs md:text-sm font-semibold border-b border-blue-600 whitespace-nowrap\">${col.label}</th>`;
            });
            html += "</tr></thead><tbody>";

            details.forEach((item, idx) => {
              const rowClass = idx % 2 === 0 ? 'bg-gray-50' : 'bg-white';
              html += `<tr class=\"${rowClass} hover:bg-gray-100 transition-colors\">`;
              columns.forEach(col => {
                let val = item[col.key];
                if (val === null || val === undefined) val = '';
                html += `<td class=\"px-4 py-3 text-xs md:text-sm text-gray-700 border-b border-gray-200 whitespace-nowrap\">${val}</td>`;
              });
              html += "</tr>";
            });

            html += "</tbody></table></div>";

            resultDiv.innerHTML = html;
          }

          function clearMnpFilters() {
            const now = new Date();
            document.getElementById('mnp-location').value = '29609';
            document.getElementById('mnp-year').value = Math.min(2025, Math.max(2017, now.getFullYear()));
            document.getElementById('mnp-month').value = now.getMonth() + 1;
            document.getElementById('mnp-result').innerHTML = '<div class="text-center text-gray-500 py-8"><i class="fas fa-database text-4xl mb-3"></i><p>Set filters and click "Refresh Report" to load M&P QSR data</p></div>';
          }

          // Initialize first tab as active
          document.addEventListener('DOMContentLoaded', function () {
            showTab('tcs');
          });

          // Sidebar toggle functionality
          function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('w-72');
            sidebar.classList.toggle('w-20');
          }
        </script>

</html>